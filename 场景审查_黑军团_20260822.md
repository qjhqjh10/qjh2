# 黑军团战场场景审查报告（2026-08-22）

> 审查对象：`d:/2/战场演示`（独立战场演示项目）黑军团场景 `battlearenablacklegion`
> 说明文件（权威）：`解包整理/07_场景/battlearenablacklegion/`（10134 个原始 Unity JSON）+ `解包整理/说明书/02_战场_场景/battlearenablacklegion.md`（598 行索引摘要）
> 完整读取：主代理读说明书全文 + 上一会话全参产物 `Warpforge_tools/data/{go_table,tree_3d,mesh_all,particles_keytable,particles_all,psr_table}_blacklegion.txt` + 2 个子代理深读（MonoBehaviour 1711 组件/AnimationClip 2/SkinnedMeshRenderer/Material 13 m_SavedProperties/RenderSettings/Camera/Light/LineRenderer/AudioSource）与粒子/网格参数 diff

---

## 一、修复差异总结（现状 vs 修复前）

### 已修复（全部有说明书依据）

| # | 说明书规定 | 修复前 | 修复后 |
|---|---|---|---|
| 1 | **ColorLookup = LUT Normal（标准 16³ identity LUT）+ contribution 1.0 + LUTBlender(_LUT2 同图,_Blend 1.0) → 黑军团无颜色分级** | lut_vignette.gdshader 写死暖调公式（r×1.04/g^0.8×1.02/b^0.75×0.95+暗部红棕染色）；`lut` uniform 设了但 shader 未声明（无效） | shader 重写为标准 3D LUT 采样（行=G 层/格=B 层/格内=R，半像素+bilinear）；暖调公式删除（identity=本色）。**此前"直采=彩虹→暖调近似"记录系误判** |
| 2 | **环境间接光 = RenderSettings m_AmbientProbe SH DC (0.289, 0.183, -0.080)（暖橙，B 为负）**——Trilight 三色只是天空盒配置 | 单色 (0.674,0.641,0.87)@0.24（Trilight 平均×自造残差） | ambient = SH DC 色（B 夹 0）energy 直通；**Trilight → Godot BG_COLOR 下 AMBIENT_SOURCE_SKY 实测无贡献（已验 ambient=0 画面不变）** |
| 3 | **Light color=(1,1,1) intensity=1.0（纯白光）；Volume 无 Tonemapping 组件 → Linear 直出** | 灯色×(1,0.957,0.839) 暖 + 0.25、tonemap=FILMIC（"暖调"依据被 #1 推翻） | 白光 1.0、TONE_MAPPER_LINEAR、glow_levels/N 只开 L6+L7（Godot 4.7 正确属性名 `glow_levels/6`、`/7`——`glow_levels=96`/`glow_level_N` 均不存在）；Bloom 1.15/5.0/Vignette 0.297/0.2 保持 ✓ |
| 4 | **Chain1/Chain2 SkinnedMeshRenderer**（mesh pid29/30 → Chain11.obj/Firepits.002.obj，单骨骼=自身，scale 376，材质=Atlans 2 Transparent） | **两条铁链完全没有**（空 Node3D） | 生成器 mesh_of 支持 SMR → 网格+材质生成，SMR 薄片面 cull_disabled（镜像下 yaw180 背面剔除）——**渲染验证 Chain1 1435px/Chain2 2131px 位置正确** |
| 5 | **Runes 材质 "Unlit UV scroll"**：_MainTex + **_SecondaryTex**(fileID9=NoiseNRM.png) + Vector4_1=(1,1,0.02,0)（"UV Scale (XY) Speed (ZW)"→副贴图 U 向 0.02/s 滚动） | 只用 _MainTex；uv_scroll=(1,1,0,0)（Vector4_1 实际在 m_Colors 不在 m_Floats，此前读空） | parse_mat 支持 _SecondaryTex + Vector4_1（m_Colors 取）→ uv_scroll.gdshader（unshaded 双层 mix + TIME 滚动 0.02/s） |
| 6 | m_IsActive=0 的 GO 整树跳过 | 生成器不检查（CrosshairLine 3D/Cache Stealth 靠 skip 列表+运气跳过） | collect 检查 m_IsActive |
| 7 | 粒子 **simulationSpeed**（光柱 0.3/毒气 0.4/烟 0.75/气体 1.5） | 全部按 1.0（光柱闪烁快 3.3×、毒气消散快 2.5×） | speed_scale 直通（39/39） |
| 8 | 粒子 **velocity-over-lifetime 恒速**（Floor Heat Grill 0,0.25,0 / Heat Distortion 0,0.2,0 / Gas(1) 0.5,0,0.5 / Energy ±6.38 x） | 写成 direction 且无初速 → **粒子钉死不动** | direction=归一化+initial_velocity=速率 |
| 9 | 粒子 **UVModule 翻页分片**（黑烟 8×8/Floor Grill 6×5/FirePit 7×7/Smoke 6×5/Gas 8×8） | 单帧整幅贴图（每颗粒显示整幅大烟→失真） | ParticleProcessMaterial.anim_speed=(fps,fps) + StandardMaterial3D.particles_anim_h/v_frames+loop（18/18 粒子生效） |
| 10 | 烟/气材质 = **Unlit 粒子着色器**（颜色=startColor×colorMod×贴图 0.2 灰） | PER_PIXEL 受光+白版贴图+暗棕 albedo（0.038,0.009,0.006）→ 双重暗化+被灯染色（Gas 亮绿变暗绿近黑） | unshaded + 原版贴图 + albedo 白（颜色链保留 startColor×colorMod） |
| 11 | **burst 类粒子 one-shot**（Skull Eyes rate=0 + burst(1) cycleCount=1 → 一次性） | amount+emitting 循环常驻（凭空多 6 个亮点） | burst-only → one_shot=true；rate=0 且无 burst → 不生成 |
| 12 | **gradient 联合时间轴**（Light Shaft 呼吸 α=1→0.53→1→0.65→1 at .2265/.447/.659） | 8 点均匀重采样（峰谷被抹平） | color/alpha 键并集时间轴逐段精确 |
| 13 | **LUT 图逐变体解析**（ColorLookup MonoBehaviour texture ref → read_obj 导出；黑军团=LUT Normal；其余 7 阵营各有 LUT Battle Arena <X>.png） | 写死检查 LUT Normal.png 存在 | 读 ColorLookup.json 的 m_Shader 引用链导出实际 LUT 图（黑军团回退 LUT Normal=identity ✓） |

### 审查确认无需修改（说明书如此）

- **粒子 39/65 差额 = 26 个战斗能量特效**（Glow Acummulated/Energy/Energy Moving Left/Accumulated Energy/Shrink glow/Energy Accumulation VFX On-Off：loop=false+delay 1-1.5，由战斗逻辑触发播放）→ 主项目 battle.gd 层控制，场景不渲染（合理）
- **CrosshairLine 3D / Cache Stealth 原版 m_IsActive=0**（inactive，不渲染）
- **Copy Mirror GO (114,-13.49,16.88) active=true 原版如此**（地下镜像副本,保留）
- **AudioSource 7 个**（3 战斗音乐 PlayOnAwake+Loop + 4 聊天空 clip）= 游戏逻辑层
- **LineRenderer 1 个（CrosshairLine 3D 战斗准星弧线, inactive）**——游戏逻辑层
- **Cinemachine Vcam 无 pipeline**（cm 组件空、无 Follow/LookAt）——相机由 clip 驱动
- **LensShift -0.205 → frustum_offset 已接入**（×0.86 换算系数为引擎等效近似,记录）

### 未修复（P1/P2，说明书记录在案）

| 项 | 说明书 | 状态 |
|---|---|---|
| **clip17 Scene Animation**（15.98s Loop PlayAutomatically：Firepits/Firepits.001 位置+旋转摆动 + **Chain1(5 形态键)/Chain2(7 形态键)** 0→99.8° 甩鞭——**float 曲线是 BlendShape 权重不是骨骼**） | P1 待接入（形态键需 Godot ArrayMesh blend shapes 构建+AnimationPlayer） |
| **clip16 Camera Intro**（2.5s：pos (3.06,2.64,9.86)→(0,2.222,-13.57) + rotX -10.405→0，**Once**，BattleManager.cameraAnim 播放） | P1（主项目 battle.gd 接入） |
| **CinemachineImpulse 相机震动**（ImpulseSource amp 0.1/freq 0.05 + 自定义 shakeSignals 4 组，战斗逻辑触发） | P1（战斗侧） |
| **Heat Distortion 折射材质**（原版屏幕空间折射专用材质） | Godot 无等效，保留 Glow 低 alpha 光斑近似（标注） |
| **网格碰撞体**（MeshCollider×9+BoxCollider×7）| P2（UI 占位/粒子碰撞） |
| **AO/烘焙 GI**（原版 Baked 静态网格+烘焙光影） | 引擎差异，P2（实时光+全局限光近似） |
| **相机动态系统**（BoardCameraScreenSize sensorX 37/41、CameraVerticalFramer viewShift 曲线、CombatCameraZoom、UnitsZoom） | 战斗运行时逻辑，P2 |
| roughness 统一 0.9 | P2 微调 |

---

## 二、修复验证（数值）

**渲染 vs 原版参考图 5×3 网格平均色差：22.3/255（≈8.7% 偏离）**

| 区域 | 参考(原版) | 当前(复刻) |
|---|---|---|
| 天空行 | (60,39,29)(160,95,41)(107,74,54)(156,100,55)(50,33,42) | (50,33,27)(98,58,36)(138,84,42)(60,42,35)(40,28,24) |
| 中景行 | (47,23,18)(106,57,31)(78,49,31)(88,46,29)(49,35,31) | (67,43,30)(105,70,44)(106,67,39)(73,48,34)(64,42,32) |
| 地面行 | (58,22,19)(86,35,29)(93,49,40)(79,38,32)(54,25,22) | (119,75,47)(107,65,47)(88,55,46)(116,72,46)(108,72,50) |

- 量级全部对齐（修复前整体 2-3 倍过曝,现 ±20-40%）
- 参考图两侧 (160,95,41)/(156,100,55) 强光斑差异=两侧火盆光晕（参考图含 UI 遮挡的区域,粒子动画帧差）
- 残余中景 G/B 偏高 10-20%（原版烘焙 GI 暖橙 vs 实时光）与地面偏亮——P2

**已证伪的指控**（vision 两次误判,均被数据驳回）：
- "粉弧/白弧"——渲染图全图粉色像素 = **0**（ID 图定位为 Banners_004/008；横幅贴图无任何粉色像素,系统 R<170 或 B<140 的橙棕被误读）
- "铁链缺失"——probe+ID 图确认 Chain1 1435px/Chain2 2131px 渲染正确（小暗目标,被 vision 漏读）

**环境光实测结论**：Godot 4.7 BG_COLOR 模式下 `AMBIENT_SOURCE_SKY` **不渲染**（ambient_energy 0/0.14/0.35 画面逐位不变,已验证）→ 环境光必须 AMBIENT_SOURCE_COLOR；三色 Trilight 无法直接表达,取 SH DC 色（说明书 RenderSettings.m_AmbientProbe 27 系数首 3 个）。

---

## 三、相关文件

- 生成器：`Warpforge_tools/scripts/unity_scene_to_godot.py`（新增：SMR 网格+has_smr/双贴图 parse_mat/_SecondaryTex/UVModule 翻页/speed_scale/恒速 vel/burst one-shot/grad 联合轴/暖调删除/Linear/SH ambient/白光灯/glow 4.7 属性/m_IsActive/逐变体 LUT 解析）
- 新 shader：`assets/lut_vignette.gdshader`（标准 3D LUT 采样+contribution+Vignette）、`assets/uv_scroll.gdshader`（Unlit UV scroll 双层滚动）
- 诊断产物：`_idmap_ids.tsv`/`out_idmap2.png`（ID 图）、`_chain_uv_region.png`/`_pink_spot.png`/`_banner_uv_region.png`
- 场景：`d:/2/战场演示/scenes/unity_arena_battlearenablacklegion.gd`（2588 行→约 2700 行）
