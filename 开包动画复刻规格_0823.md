# 开包动画复刻规格（Booster Pack Open Window + Rarity 粒子层 + RewardAppearParticle）

> 2026-08-23 生成 · 只读研究 · 依据：原始 Unity JSON（`D:/2/解包整理/03_界面UI/菜单/`、`09_游戏数据/卡包/`、`03_界面UI/去重资源/`、`03_界面UI/共享资源/`）+ 反编译 C#（`D:/2/Warpforge_code/Scripts/Assembly-CSharp/{BoosterPackOpenWindow,CardInBoosterPack,BoosterPackBackground}.cs`）+ 子代理复核
> 坐标一律 Godot 1920x1080、y 向下（Unity y 向上已翻转：godot_y = 1080 - unity_y - h，见 chain_rect 换算）

---

## 0. 一句话总览

原版开包 = **五张大卡（每张 377.5×513.4px）从屏心叠放上升 → 十字扇形展开 → 玩家逐张点击 → 按稀有度播放不同时长的"3D 翻转"+ 稀有度粒子爆发 + 相机震动 + 稀有度配色 + 音效 → 5 张全开后"Tap to close" 闪烁提示 → 点击后 5 卡飞向屏幕下方收拢 → 窗口关闭**。整个流程共 3 段动画剪辑 + 6 个粒子层（4 个稀有度层 + 窗口星尘 + Reward 烟尘）+ 卡组内部 3 段翻面剪辑。

---

## §1 原版权威规格

### 1.1 窗口结构树（Booster Pack Open Window，菜单全树.md 行 8040，节点 8040–8352）

GO 根 `Booster Pack Open Window`（全屏；(0,0) 锚点拉伸，pivot 0.5,0.5；组件：RectTransform + `BoosterPackOpenWindow` 脚本 + **Animation** 组件 [windowAnimation]）：

| 节点 | Godot 区域/参数 | 说明 |
|---|---|---|
| `Booster pack Background` | x -100..2020, y -100..1180（2120×1280 居中） | Image 用 **40K_shop_offer_bg_Sororitas_0.png**（项目已有）；`Gradient2`（暗角：灰0.764→黑，_zoom 1.22，_blendMode 2，modifyVertices）；`AnimateUIMaterialProperty`（_Distort_Scale / _Distort_Strength，开窗抖动用）；`BoosterPackBackground`（`thereIsALegendaryCardbackgroundColor`=(1.514, 0.459, 0.0) —— **包内有传说卡时整图染金色**）；`UIAnchorAvoidSafeArea`；Animation=[背景抖动剪辑] |
| `Booster Animation Parent` | (910,490)-(1010,590) 100×100 居中 | 卡与包的锚挂点（boosterPackAnchor）；RT anchor(0.5,0.5) pivot(0.5,0.5) |
| `Cards` | 同父 100×100 | 嵌套 **Canvas** + **GraphicRaycaster**；RT anchor(0.5,0.5) |
| `CardInBoosterPack UI 1..5` | RT size **2.5×3.4**（运行时 localScale 151 → 屏上 **377.5×513.4**），anchor(0.5,0.5) pivot(0.5,0.5) | 每卡组件：RT + `CardInBoosterPack` 脚本 + **Animation**(cardAnimation) + CanvasRenderer；内容 `Content`(RT) → `CardUI`(完整 BasicCardUI：2DCard→Front→CardImage/CardFrame/Card Info(Health/Attack/Cost/Rarity Sprite…)+Cardback Container[inactive]+New Card Badge[inactive]+Ban Icon) |
| `Tap to discover` | (1157,977) 708.3×103，anchor(1,0) pivot(1,0) | TMP 文字 **'Tap on the cards to discover'**，字号 **49.82**；Localize 词条 MenuShop/BoosterPackOpening/TapToDiscover |
| `Tap to close` | 同 (1157,977) 708.3×103 | TMP 'Tap to close' 字号 49.82 + **BlinkGraphic**(blinkSpeed 1.0, colorVariation 0.5) + UIGenericEventCatcher；子 `Collider`=NonDrawingGraphic 全屏碰撞（offsets -2045.6..1807.5 x, -999.5..1233.1 y） |
| `Booster pack stars` | Transform 粒子（无 RT）pos(0.5,0,-2.0)，rot 180°X | 「Flare 6 Additive」星尘背景，见 1.7 |

窗口脚本关键字段：`cards[]`(CardInBoosterPack×5，注释"order from top left to bottom right")、`closeButton`、`soundOnClose`、`tapHelperText`、**`timeToShowHelpText=10.0`**、`windowAnimation`、`closeAnimation='Booster Window Close'`、`boosterPackAnchor`、`boosterPackBackground`、`useQuickOpenProtection(快速连开保护：拖 0.15s/次、上限 2s、60s 归零、4 次内不拖)`。

### 1.2 时序 A：窗口开启（0→0.617s）

AnimationClip **`Booster Window Open`**（菜单/AnimationClip，pid 5911697182262325120）——目标路径 `Booster Animation Parent/Cards/CardInBoosterPack UI n`：

| t(s) | 事件 |
|---|---|
| 0.000 | Cards 隐藏（m_IsActive=0） |
| 0.017 | Cards 激活；5 卡**叠放于屏心** (960,540)，localScale=157.58 |
| 0.017→0.217 | 5 卡**整体升起 256px**（anchoredPos y 0→256，即 Godot (960,284)；x=0），scale 保持 157.58（进场过冲） |
| 0.217→0.617 | 5 卡**十字扇形展开**到最终位（scale 157.58→**151**；localPosition.z=129，深度项 Godot 忽略）： |

**最终 5 卡屏心坐标（Godot，卡片中心）**：

| 卡 | anchoredPos(Unity) | 屏心(960,540)+ | Godot 中心 | 卡片范围 x | 卡片范围 y |
|---|---|---|---|---|---|
| UI 1（左上） | (-425, +210) | (535, 330) | **(-425,+210)** | 346..724 | 73..587 |
| UI 2（上中） | (0, +318) | (960, 222) | **(0,+318)** | 771..1149 | -35..479 |
| UI 3（右上） | (+425, +210) | (1385, 330) | **(+425,+210)** | 1196..1574 | 73..587 |
| UI 4（左下） | (-212.5, -338) | (747.5, 878) | **(-212.5,-338)** | 559..936 | 621..1135 |
| UI 5（右下） | (+212.5, -326) | (1172.5, 866) | **(+212.5,-326)** | 984..1361 | 609..1123 |

（即：上方 1 张居中偏上、左右各 1 张中段、下方左右各 1 张——"十"字错位布局，非并排横列。）

**并行**：`Booster Window - Background Shake On Open`（0.917s）：_Distort_Scale -2→0.2(t0.583)→0.4(t0.917)；_Distort_Strength 0→0.35(t0.217)→0(t0.917)——背景抖动开启时播放。
窗口打开后在 **0.617s** 触发 AnimationEvent（AnimationEndEvent）→ 卡可点击、显示 "Tap on the cards to discover"（helpText 10s 后自动隐藏/或点击后隐藏）。

### 1.3 时序 B：逐卡点击翻面（点击卡片 i）

`CardInBoosterPack.CardClicked()`：
- 状态机：NonInitialized → **(Initialize) Positioning** → (等 animDelay) **ReadyToOpen** → (**点击**) **WaitingForOpening** → (剪辑 AnimationEvent **FinishedOpeningAnimEvent**) **Opened** → 播放 `Booster Opening - Card Idle`（8s 漂浮循环，Content ancPos x ±0.04/y ±0.05 单位 ×151≈**±6~7.5px 呼吸浮动**）
- 按 `contentByRarities[rarity]` 取：**翻面动画剪辑 + 音频Cue + 粒子层(GameObject) + 相机震动预设**。

**contentByRarities 权威表**（CardInBoosterPack UI 1 MB 序列化，5 卡同表）：

| CardRarity | 枚举 | 翻面剪辑 | 时长 | 粒子层(GO pid) | 音频 | 相机震动 |
|---|---|---|---|---|---|---|
| None(0) | 0 | `Booster Opening - Card Open Normal` | 0.667s | Rarity 1 (-8429409561944019767) | Cardflip_Common | 无 |
| Common(1) | 1 | Normal 同上 | 0.667s | Rarity 1 | Cardflip_Common | 无 |
| Rare(2) | 2 | Normal 同上 | 0.667s | Rarity 2 (123795767370143701) | Cardflip_Rare | delay 0.65s, amp 5, freq 0.1 |
| Epic(3) | 3 | **`Booster Opening - Card Open Rare`** | **1.167s** | Rarity 3 (3084704502879738129) | Cardflip_Epic | **delay 1.0s, amp 40**, freq 0.1 |
| Legendary(4) | 4 | `Booster Opening - Card Open Legendary` | **1.5s** | Rarity 4 (8268263308888823471) | Cardflip_Legendary | shake①: 预设A delay 0, freq 0.25；shake②: delay 1.15s, amp 20, 方向(0,0,10), decay 0.5 |
| Special(5) | 5 | `Card Open Rare` | 1.167s | Rarity 4 | Cardflip_Epic | 无 |

**翻面剪辑细节**（统一：旋转 `Content` 绕 Y 轴 + 上半周期反向"预备" + 下半周期转到 180°，在 90° 瞬间 scale.x→-1（= 深度翻转技巧，视觉效果 = scale.x=cosθ(t)，即古典 2D 翻面曲线），同时代码在 90° 翻转点把卡背换成卡面）：

| 剪辑 | 预备(anticipation) | spin 起点/终点 | scale.x=-1 时刻 | scale 下压 0.95 |
|---|---|---|---|---|
| Normal (Common/Rare) | t0→0.25s：Y 轴 -30° 后回 0 (t0.317) | 0.317s→**0.667s 转 180°** | t0.45（90° 时刻） | 无 |
| Rare (Epic) | 前 0.5s 静置（无预备）→ t0.3 起 -30°@0.433 回 0 | 0.5s→**1.167s 转 180°** | t0.717 | t0.5（下压 0.95 至 t0.7 回 1） |
| Legendary | t0.567 起 -40.6°@t0.75 回 0 (t0.833) | 0.833s→**1.5s 转 180°** | t1.083 | t0.833（0.95→t1.067 回 1） |

（Normal 曲线为 41 个 60fps 关键帧线性 0→30°→0→180°：θ(t)：0.25s@-30° → 0.317s@0° → 0.667s@180°。Rare 为 71 帧、Legendary 91 帧，详见原始 JSON `AnimationClip_*.json`。Rare/Legendary 的 scale 下压=翻牌"顿挫"。）

点击也播放 `clickSound`(3481876391266756950)；若为新卡播放 `newCardSound`(4962178071637942874)。

### 1.4 时序 C：全部翻开 → 关闭（Booster Window Close）

- 5 张全 Opened 后：TapHelper 文本切换为 "Tap to close"（BlinkGraphic 1s/s 闪烁），显示全屏 Collider 可点击；BIG：**无下卡面板/无总结弹窗**——直接关闭。
- `Booster Window Close` 剪辑（0.567s，路径同上）：5 卡**飞向屏幕下方中心**（anchoredPos → (±1..±4, **-990**)，即 Godot y≈1530 屏外下方），卡间错开：UI1 0.567s、UI2 0.483s、UI3 0.4s、UI4 0.317s、UI5 0.25s（右快左慢收拢）；scale 不变（仍 151）；收拢伴随 closeSound。
- 也可点右上 Close 按钮（UIGenericEventCatcher）关闭（本窗口无需，游戏里改由 tap-to-close 全屏）。

### 1.5 粒子层元素表：Boosterpack Open Card Rarity 1–4

4 个层根 GO 均为「Transform + ParticleSystem(根闪光) + PSR」，本地 pos(0,0.061,0)、rot **-90°X**、scale 1；运行时由 `CardInBoosterPack` **Instantiate 到翻牌卡上**（卡 localScale=151 → 粒子尺寸 ×151 才是屏上像素；子节点 pos z=-0.043 ≈ 6.5px 前置）。**全部 playOnAwake、single-burst（m_BurstCount=1，burst 次数未序列化——提取管线丢弃，实施时按视觉调 N，见 §3）**，colorOverLifetime 全线 = 白→透明（线性渐隐），材质全部 Additive（m_CustomRenderQueue 3000）。

**通用材质→贴图映射**（已从 Material JSON 确认）：
- `Spark Additive Extra Color` → **Spark.png** (去重资源 64×64)，_ExtraColor=1.5
- `Sparkle Additive Extra Color` → **sparkle_blue 1.png** (去重资源 256×256，**2×2 四帧动画**)
- `Glow Rays Additive Extra Color` / `Glow Rays` → **Glow Rays.png** (去重资源 128×128)
- `Glow Additive Extra Color` → **Glow.png** (去重资源 128×128)，_ExtraColor=1.5
- `CardFlip Effect` → **CardFlip.png** (菜单 256×256)
- `Aura_2` → **Aura_2.png** (共享资源 128×128)
- `Smoke5_extra color` → **Smoke5.png** (去重资源 1024×1024，**8×8=64 帧**)
- `Flare 6 Additive` → Flare 6 Additive.png（**项目已有**）
- `Smoke Cloud With Mask` + 噪声贴图 → Smoke Cloud With Mask.png (菜单 128×128) + Noise Combined.png

#### Rarity 1（Common/None）

| 元素 | 材质 | 关键参数（原始单位，×151=px） |
|---|---|---|
| 根 `Boosterpack Open Card Rarity 1`(flash) | Glow Rays Additive | 单次爆发；startDelay **0.25**；startSize **8.71**；life 0.38；SizeOverLife 曲线 0→1（线性放大）；白→透明淡出；renderMode Billboard |
| `Sparks` | Spark Additive | delay 0.25；life **1.0..2.0**；size **0.05..0.1**；**Shape=SpriteRenderer(type18)**，scl(1.78,2.75)（≈卡背内区 2.1×3.3 的 85%），angle 25°（发射锥），arc 360°，rot(90,0,0)；**Noise strength 0.5** freq 0.5（飘散）；无速度 |
| `Sparks (1)` | Spark Additive | delay 0.25；life **0.1**；size 0.05..0.1；同形状；**VelocityOverLife x=10**（定向喷射 +x）+ Noise 无；快速飞散 |
| `CardFlip` | CardFlip Effect | delay 0.25；life **0.5**；size 1.0；**renderMode=4 Mesh**（Quad，Mesh pid -4502588434505249381，贴图 CardFlip.png）；**RotationOverLife y 0→-2π（整圈自转）**；sortingOrder **3**（盖在卡上） |

#### Rarity 2（Rare）

| 元素 | 参数（与 R1 不同处） |
|---|---|
| 根 flash | Glow Rays；delay 0.25；size 8.71 同 R1 |
| `Sparks` | 同 R1，**Noise strength 0.7**（quality 2） |
| `CardFlip` | 同 R1 |
| `Glow expand shrink` | Glow Rays；delay **0.2**；life 1.0；size **8.71**；SizeOverLife [0:0, **0.29:1**, 1:0.383]（快爆慢缩）＋ **RotationOverLife ±45°(0.785rad)** 扫转；白→透明 |

#### Rarity 3（Epic）

| 元素 | 参数 |
|---|---|
| 根 flash | **Glow Additive**（软辉光）而非 Rays；delay **0.4** |
| `Sparks` | Spark；delay 0.4；Noise strength **1.0** quality2 |
| `Glow expand shrink` | Glow Rays；delay 0.4；size 8.71；SizeOverLife [0:0,0.29:1,1:0.383]；sortingOrder 2 |
| `Sparks 2` | Spark；delay **0.5**；life 1.0..2.0；**speed 10..5**；size 0.05..0.1；**Shape=Sphere(type0) radius 0.5**；Noise 0.5；**ClampVelocity：magnitude 1→0，dampen 0.2**（喷射减速） |
| `CardFlip` | CardFlip Effect；delay **0.5**；life **0.6**；sorting 3 |
| `BG Glow` | Glow Rays；delay **0.0**；size **5.5**；SizeOverLife [0:**0.515**,1:1]；Rot ±30°；sortingOrder **1**；pos(0,-0.26,-0.04)（卡后下方衬光） |
| `Glow lingering` | Glow Rays；**loop**；delay 0.6；life **4.0**；size 5.0；SizeOverLife [0:0,**0.5:1**,1:0]（渐入渐出脉冲）；Rot ±20°；sorting 1 |
| `Glow expand shrink (1)` | Glow Rays；delay 0.4；size **3.0**；SizeOverLife [0:0,0.29:1,1:0.383]；sorting 2 |

#### Rarity 4（Legendary）

| 元素 | 参数（差异） |
|---|---|
| 根 flash | Glow Additive；delay **0.6** |
| `Sparks` | Spark；delay **0.8**；Noise 1.0 |
| `BG Glow` | Glow Rays Additive；delay 0；sorting 1；pos(0,-0.26,-0.04) |
| `Glow expand shrink` | Glow Rays；delay 0.6；size 8.71；sorting **3** |
| `Rings expand` | **Aura_2**（环纹贴图）；delay 0.8；life 1.0..0.7；size **5..6**；SizeOverLife 0→1；Rot ±45°；sorting 3（扩散光环） |
| `Sparks 2` | Spark；delay 0.8；speed 10..5；**Sphere r=0.55**；Noise 0.5；dampen **0.12** |
| `CardFlip` | CardFlip Effect；delay 0.7；life **0.8**；sorting 3 |
| `Sparks (2)` | **Sparkle Additive**（sparkle_blue 2×2 四帧）；delay 0.8；rate **5/s loop**；life 0.5..1.0；size **0.1..0.3**；SizeOverLife [0:0,0.5:1,1:0]；TextureSheet 2×2 fps30 frameOverTime 0.25（闪星持续闪烁） |
| `Glow lingering` | 同 R3（loop，life 4.0，delay 0.6） |
| `Glow expand shrink delay` | Glow Rays；delay **1.0**；SizeOverLife **[0:1,1:0]**（反向：从满缩没）；size 8.71；sorting 3 |
| `Glow expand vertical` | Glow Rays；delay 0.6；size **3.0**；same expand-shrink 曲线；sorting 3 |
| `Smoke ring` | **Smoke5**（8×8=64 帧）；delay **1.0**；life 2.0；speed **5..4**；size **1.5..0.5**；Shape **SingleSidedEdge(type10) radius 1.5 thickness 0.12**（环状）；TextureSheet 8×8 fps30 frameOverTime 0.046875；dampen 0.2（后期烟环） |

#### RewardAppearParticle（菜单行 29；**归属 Reward Window**，非开包窗口——奖励窗口弹出奖励时用）

- 根：**scale 14.838**，rot -180°X；sortingLayer **7** (1054366423)；材质 `Smoke Cloud With Mask`（+Noise 贴图）；life 0.5..1.5；speed 2..3；size 1.5..2.5；Shape SingleSidedEdge r0.76 t0.52；ClampVelocity magnitude [0:1, **0.04:0.078**] dampen 1.0
- 子 `Glow (inactive)`：Glow Additive；scale **50.905**；size 1.5..2.5；SizeOverLife 0→1。——**默认不激活**。

#### Booster pack stars（窗口背景星尘）

- `Flare 6 Additive`；**loop | prewarm | simSpeed 2.0**；life 7.0；speed 3..7；size 1.0；max 5000；rate **100/s**；Shape SpriteRenderer scl(**120.6, 65.1**)；OrbitalVelocity z 0.015..0.01（缓慢绕旋）；SizeBySpeed [0:0.2,1:1]。位置 (0.5,0,-2.0)、rot 180°X。→ Godot：屏幕中央大矩形发射区的闪烁星海，背景层常开。

### 1.6 开包图缩放+爆炸（"Booster Open Standard"/"Sororitas" 两剪辑，卡包 bundle）

包艺术（BoosterPackVisualItem，阵营包 `Booster Pack Standard <Faction>`，含 `Booster Pack Particles`(→`ParticleExplosion`)、`Force`、`Army Text` 等）在开窗时实例化到 `Booster Animation Parent` 下并播放：
- **SCALE 0.1 → 159.4 @t0.717**（包体从微缩冲到全屏）
- ROT：0→~0.5s 渐转 3D 翻滚（终态 ≈ 四元数(0.23,-0.92,-0.33)，约 166° 大角度倾转+滚转），t0.5→2.0s 微摆
- material._ExplodeRange 1.0→2.367s@0；_InflateDistance 0→1(**t1.5** 波浪点)（囊体吹胀）
- `Booster Pack Particles` m_IsActive @t0.667；`Booster Pack Particles/ParticleExplosion` @**t1.983**；`Force` 闪光 @t2.0；`Army Text` @t0.6-0.75 淡出
- 全长 2.367s。Sororitas 变体 _ExplodeRange 1.75s@0。
- 附带 `Open_Cardpack_<Faction>.wav` 音效（13 阵营 + Generic）。

### 1.7 卡背/卡面切换

卡 UI 默认 Cardback Container **inactive**（初始化时由代码开卡背）；点击翻转：卡内容 `Content` 旋转到 90° 时换面（即时），翻完显示卡面+卡框（`Card Rarity Sprite` 常用 `1_40k_cardframe_rarity_common.png`，翻出后按稀有度换 40k_cardframe_rarity_*.png——项目已有这 4 张）+ `CardRarityColorsSO` 稀有度色（染色卡背高亮/粒子）：
- r0 白 / r1 Common (0.037,0.261,0.311)a0 / r2 Rare **(0.072,0.566,0.056)a0.667** 绿 / r3 Epic **(0.565,0.055,0.526)a0.667** 紫 / r4 Legendary **(1.282,0.470,0.0)a0.667** HDR 橙金 / r5 Special (0.824,0.161,0)a0.639

---

## §2 项目现状 vs 原版差异清单（d:/warpforge/scripts/packs.gd）

| # | 项目现状 | 原版 | 缺什么 |
|---|---|---|---|
| 1 | 5 卡 180×240 并排横排，x0+i*260 一字排开 | 377.5×513.4 大卡，**十字错位扇形**（上中+左右中+下左右），入场叠放→上升 256px→散开 | 布局/尺寸/入场级联 |
| 2 | 卡直接显示卡背于 (960-90,420) | 全部先叠放屏心 → 升起 → 散开 | 叠放→展开动画（0.617s） |
| 3 | 无背景图/暗角/开窗抖动 | 40K_shop_offer_bg_Sororitas_0 + Gradient 暗角 + 开窗 Distort 抖动 | 背景层全部 |
| 4 | 无星尘背景 | Flare 6 星海长开（rate100 simSpeed2） | Booster pack stars |
| 5 | 无包体放大/爆炸 | 包艺术 0.1→159.4 膨胀 + ParticleExplosion @1.983s + Open_Cardpack 音效 | 开包爆炸动画 |
| 6 | 无传说包橙金染色 | 包内有传说→背景图染 (1.514,0.459,0) | 金字背景染色 |
| 7 | 翻面 = scale.x 0.05→swap→1（0.18s） | 三段式剪辑：预备(-30°/-40°)→Y轴旋转 180°（0.667/1.167/1.5s）→scale.x=cosθ；**没有版本差异延迟与顿挫** | 按稀有度时长曲线 + 预备/顿挫 |
| 8 | 无任何粒子 | 4 稀有度层（flash+Sparks(+Noise)+Glow expand/lingering+Rings+Smoke ring+Sparkle loop+CardFlip 转片……） | 全部粒子层 |
| 9 | SFX.play_cardflip 已有（Cardflip_*.wav 4 张已在项目） | Cardflip_Common/Rare/Epic/Legendary ✓ | —（已到位） |
| 10 | 无相机震动 | Rare amp5@0.65 / Epic amp40@1.0 / Legendary 双段 | 震动（Godot 相机 shake 实现） |
| 11 | 无稀有度配色 tint | CardRarityColorsSO（r2 绿/r3 紫/r4 橙金） | 翻出后卡框/高亮配色 |
| 12 | 翻完显示 "Pack opened! 卡片列表" 文本 + 全屏关闭 | 翻完直接 "Tap to close"（49.82 号字闪烁）+ 点击收拢飞下 | 收尾交互 |
| 13 | 关闭 = 结果层直接隐藏 | 5 卡错时飞向屏幕下方（-990）0.25-0.567s + 音效 | 收拢动画 |
| 14 | 无 idle 呼吸 | 翻完卡播 Idle 8s 漂浮（±6-7px） | 待机动画 |
| 15 | 卡图片 900×1200 换面（face_hi） | 卡 UI 内嵌 CardUI（框/信息/角标） | 可选（细化） |

---

## §3 实施规格（Godot，d:/warpforge）

### 3.1 资源入库（assets 拷贝清单，源=解包）

| 文件 | 源路径 | 目标 |
|---|---|---|
| Spark.png | 03_界面UI/去重资源/Texture2D/Spark.png | assets/particles/Spark.png（新） |
| Glow Rays.png | 同上 | assets/particles/Glow Rays.png（新） |
| Glow.png | 同上 | assets/particles/Glow.png（新） |
| sparkle_blue 1.png | 同上 | assets/particles/sparkle_blue 1.png（新） |
| Smoke5.png | 同上 | assets/particles/Smoke5.png（新） |
| CardFlip.png | 03_界面UI/菜单/Texture2D/CardFlip.png | assets/particles/CardFlip.png（新） |
| Aura_2.png | 03_界面UI/共享资源/Texture2D/Aura_2.png | assets/particles/Aura_2.png（新） |
| Smoke Cloud With Mask.png | 03_界面UI/菜单/Texture2D/Smoke Cloud With Mask.png | assets/particles/（RewardAppear 用，新） |
| Flare 6 Additive.png | （项目已有 ✓） | — |
| 40K_shop_offer_bg_Sororitas_0.png | （项目已有 ✓） | — |
| Open_Cardpack_*.wav ×14 | 04_音频/音效库/AudioClip/Open_Cardpack_*.wav | assets/audio/ui/（新，13 阵营+Generic） |
| Cardflip_*.wav | （项目已有 ✓） | — |

### 3.2 粒子实现方案

**推荐**：GPU 粒子用 `GPUParticles2D`（CPUParticles2D 亦可，量小）：每元素一个节点，`one_shot=true`、`emitting` 手动触发、`amount=N`（burst 数量——原始 JSON 未存 burst 次数，按材质/面积观感取：Sparks N=40~60、根 flash N=1、Glow N=1、Sparks(1) N=20、Sparkle(loop) rate 5/s、Smoke ring N=12、Rings N=1、BG Glow N=1、Glow lingering N=1、Sparks 2 N=30）。**所有 size 参数 ×151**（卡片单位→屏上像素；若做"卡片 Control=377.5×513.4 直尺像素"，则粒子挂卡内、参数直接 ×151）。

粒子清单（Godot 节点参数，命名随原版）：

| 节点 | texture | amount | lifetime | size(px,×151) | emission shape | 其他 |
|---|---|---|---|---|---|---|
| RarityFlash (root) | Glow Rays/Glow | 1 | 0.38 | 1315 | point | scale curve 0→1 over life; 白→透明α; delay 0.25/0.4/0.6 |
| Sparks | Spark | 45 | 1-2s | 7.5-15 | card rect (±89×137 即 1.78/2×151, 2.75/2×151 半宽高) + spread 25° | noise 0.5/0.7/1.0; delay 0.25-0.8 |
| Sparks1 (R1) | Spark | 25 | 0.1 | 7.5-15 | 同上 | velocity (1510,0)（10×151） |
| Sparks2 (R3/R4) | Spark | 30 | 1-2 | 7.5-15 | sphere r=75.5 | speed 1510..755, dampen 0.2/0.12, magnitude 1→0 |
| CardFlip | CardFlip.png | 1 | 0.5/0.6/0.8 | 151 | point | rotationOverLife -360°; z 前 6.5px; sorting 在卡上 |
| GlowExpandShrink | Glow Rays | 1 | 1.0 | 1315 (R2/R3/R4), 453 (size3.0) | point | scale curve [0:0,0.29:1,1:0.383]; rot ±45°; delay 0.2/0.4/0.6 |
| BGGlow | Glow Rays | 1 | 1.0 | 830 | point | scale [0:0.515,1:1]; pos 卡内 (0,+39px)（-0.26×151 下方） |
| GlowLingering | Glow Rays | 1 | 4.0 loop | 755/830 | point | pulse [0:0,0.5:1,1:0]; delay 0.6; rot ±20° |
| RingsExpand | Aura_2 | 1 | 1.0..0.7 | 755..906 | point | scale 0→1; rot ±45°; delay 0.8 |
| SparkleLoop (R4 Sparks2) | sparkle_blue 2×2 | 5/s loop | 0.5-1 | 15-45 | card rect | anim 2×2 @30fps; delay 0.8 |
| GlowExpandShrinkDelay (R4) | Glow Rays | 1 | 1.0 | 1315 | point | scale [0:1 → 1:0]（缩没）; delay 1.0 |
| GlowVertical (R4) | Glow Rays | 1 | 1.0 | 453 | point | expand-shrink; delay 0.6 |
| SmokeRing (R4) | Smoke5 8×8 | 12 | 2.0 | 226..75 | ring (单边环形 r=226, 厚 18px) | speed 755..604; sheet 64 帧 @30fps; delay 1.0 |
| RewardAppearRoot | Smoke Cloud With Mask | 1 | 0.5-1.5 | 226..377 (×1) | ring r 0.76 厚 0.52（节点自身 scale 14.838） | speed 2..3; 质量粘滞 dampen 1.0; sortingLayer 高层 |
| BoosterStars | Flare 6 Additive | 100/s loop | 7.0 | 151 | big rect 120.6×65.1 单位 → 屏大小 | simSpeed 2; orbit z; sizeBySpeed; prewarm |
| (Reward) Glow | Glow Additive | 1 | 0.5 | ×50.905 | point | 默认 OFF |

全部粒子 `BlendMode=Add`，texture 开 `radiance/normal`（按原 Additive 材质），颜色白→透明（粒子的 color ramp 用渐变贴图或 modulate）。

### 3.3 与 packs.gd 集成（状态机与编排）

新建 `scripts/booster_open_window.gd`（或 packs.gd 内 `_show_opening_animation` 改写），结构对齐原版：

```
BoosterPackOpenWindow (Control, fullscreen)
├─ BgImage (TextureRect: 40K_shop_offer_bg_Sororitas_0, 2120×1280 居中)
│  └─ (legendary→modulate=(1.514,0.459,0))(Gradient 暗角可后置)
├─ Stars (GPUParticles2D 星海)
├─ PackArt (开包爆炸层: 包图缩放 0.1→159.4 + 爆炸粒子@1.983s + Open_Cardpack 音效)
├─ Cards (中心容器 100×100@(960,540))
│   ├─ Card1..5 (Control 377.5×513.4 pivot(0.5,0.5))
│   │    ├─ Back(卡背)/Face(卡面+CardFrame)+
│   │    └─ RarityFX 层 (按稀有度 实例化 GPUParticles2D 组)
├─ TapHelper (Label 49.82px: 'Tap on the cards to discover' / 'Tap to close' + Blink)
└─ TapToCloseCollider (全屏 NonDrawing)
```

时序编排（GDScript，全部可用 `create_tween`）：
1. **Open**（t0）：Bg 抖动画（distort 0.35, 0.917s）+ Stars 启动 + PackArt 膨胀（0.717s）→ @0.617s Cards 显示（scale 157.58 → 151）、TapHelper('Tap on the cards to discover') 显示；
2. **Fan-in**：Cards 从叠放 (960,540) 升到 (960,284)（0.2s）→ 散向 5 个终点（0.4s，Back ease-out）：(535,330)/(960,222)/(1385,330)/(747.5,878)/(1172.5,866)（rotation 可加 ±3°微倾，原版无 rotation 曲线——保持 0）；
3. **Tap card** → CardInBoosterPack 状态机：play `sfx_cardflip(rarity)`；按表 1.3 选剪辑时长（0.667/1.167/1.5s）：**scale.x = cos(θ(t))**（θ: 用 tween 序列：预备±30~40°→0→线性转到 180°，在 θ=90° 时换卡背→卡面（tex 换 face_hi + frame 换 rarity 框 + tint CardRarityColorsSO 色）+ 实例化 RarityFX 粒子组到卡位置（各粒子 delay 已含在参数中）+ 相机震动（[amp 5@0.65]/[amp 40@1.0]/[0@0 20@1.15 dir z]）；剪辑完毕 → 播 Idle（8s ±7px 呼吸循环）+ 记 Opened；
4. **All 5 opened** → TapHelper → 'Tap to close'（BlinkGraphic：modulate 闪烁 speed 1.0, variation 0.5）+ 全屏 TapToClose collider 激活；
5. **Close**（点击）：每卡错时（0.567/0.483/0.4/0.317/0.25s）飞向 (960, 1530)（屏下，y -990 换算）渐隐 + closeSound → 结果层隐藏；**注意：不开"汇总面板"**——原版没有。

SFX 映射：rarity→`SFX.play_cardflip()`（已有）；开包爆炸 `Open_Cardpack_<Faction>.wav`（需复制+注册）；close `CardMove`（已有）。

### 3.4 复现注意（坑）

1. **粒子尺寸 ×151**（卡 localScale）——别忘；Sparks 发射区=卡背面内区 (±1.78/2, ±2.75/2)×151 = (±134, ±208)px。
2. CardFlip 渲染层级 = 卡上方（sorting 3）；Glow lingering/BGGlow sorting 1（卡下衬光）。
3. burst 数量 JSON 丢失（m_BurstCount=1）——§3.2 给了推荐值，落地后整屏截图与原版（说明书预期）比对着调。
4. `UIGenericEventCatcher`(Tap to close) 与 `NonDrawingGraphic` 全屏 collider 是**不可见**交互层。
5. 颜色：Legendary 背景染色 (1.514,0.459,0) 是 **HDR**（>1）——Godot 用 modulate (1.5,0.46,0)+白底色即可，勿 clamp。
6. 翻面 90° 瞬间换面 + scale.x=-1 深度翻转 = 等价于 Godot `scale.x=cos(θ)` 一条曲线（θ 过 90° 后为负 = 自然镜像显示背面→正面）。
7. 原版在"快速连开"时有 0.15s/次 的防连点延迟（上限 2s）——packs.gd 已有 `_opening` 防抖，可忽略。
8. The 开包窗口的卡是 **RawCardScript 运行时数据**驱动（卡图/帧/稀有度），Godot 用 `GameData.get_face_hi()` 已有。

---

## §4 资源可用性检查

| 资源 | 解包 | 项目 | 状态 |
|---|---|---|---|
| Spark.png 64×64 | 03_界面UI/去重资源/Texture2D/ ✓ | assets/particles/ ✗ | **需复制** |
| Glow Rays.png 128×128 | ✓ | ✗ | 需复制 |
| Glow.png 128×128 | ✓ | ✗ | 需复制 |
| CardFlip.png 256×256 | 03_界面UI/菜单/Texture2D/ ✓ | ✗ | 需复制 |
| Aura_2.png 128×128 | 03_界面UI/共享资源/ ✓ | ✗ | 需复制 |
| Smoke5.png 1024×1024(8×8 帧) | ✓ | ✗ | 需复制 |
| sparkle_blue 1.png 256×256(2×2 帧) | ✓ | ✗ | 需复制 |
| Smoke Cloud With Mask.png 128×128 | 菜单 ✓ | ✗ | 需复制（Reward 用） |
| Flare 6 Additive.png | — | assets/particles/ ✓ | 已有 |
| 40K_shop_offer_bg_Sororitas_0.png | ✓ | ✓ | 已有 |
| Cardflip_Common/Rare/Epic/Legendary.wav | 04_音频 ✓ | assets/audio/ui/ ✓ | 已有 |
| Open_Cardpack_<13 阵营>.wav + Generic | 04_音频/音效库/AudioClip/ ✓ (14 个) | ✗ | 需复制 |
| 40k_cardframe_rarity_{common,rare,epic,legendary}.png | ✓ | assets/ui/mainmenu/scenes_sprites/ ✓ | 已有 |
| 卡背 Cardback_<阵营>_*.png | ✓ | assets/deco/cardbacks/ ✓ | 已有（packs.gd 在用） |

**转换器可用性**：`convert_unity_particles.py --3d/2d` / `convert_card_vfx_bundle.py` 均只覆盖 `08_预制体特效` 战斗 VFX bundle；开包粒子在 **03_界面UI/菜单** bundle（battle VFX 之外），且 burst 参数在提取时已丢失 → **converter 不适用，需按 §3.2 手写 GPUParticles2D**（参数全部已在上表；贴图直接拷 PNG 到 assets 即可，无需转格式）。`06_特效_预制体` 说明书无 Booster 条目（977 根清单=战斗 VFX），本粒子层应作为新条目记入使用地图（开包粒子根 GO 5 个（Rarity1-4+Stars+RewardAppear））。

---

## 附：关键原始文件索引（复核用）

- 窗口树：`03_界面UI/菜单/GameObject/Booster Pack Open Window_3599444745529343011.json`（子：Booster pack Background/Booster Animation Parent/Cards/CardInBoosterPack UI 1-5/Tap to discover/Tap to close/Booster pack stars）
- 窗口脚本：`09_游戏数据/脚本定义/MonoScript/MonoScript_-7564902240031634947.json`（BoosterPackOpenWindow；卡脚本 -3905637978343207600=CardInBoosterPack；-7254427082458750027=CardRarityColorsSO）
- 剪辑：`03_界面UI/菜单/AnimationClip/*.json`（Booster Window Open 5911697182262325120 / Close -8254079481253810722 / Background Shake -177604310092043705 / Card Open Normal 8301228418192668656 / Rare -9033435933554767437 / Legendary -1691631367813043119 / Card Idle -7993935874865337289 / OpenCardbacks 7721407185759151047）；`09_游戏数据/卡包/AnimationClip/{Booster Open Standard 7302051762796698856, Booster Open Sororitas 5156805757413551419}.json`
- 粒子层：`03_界面UI/菜单/GameObject/{Boosterpack Open Card Rarity 1..4|-...json}` + `ParticleSystem/*.json`；材质 `03_界面UI/去重资源/Material/Material_-8031580538284717323.json`(Spark Additive Extra Color) 等
- C#：`D:/2/Warpforge_code/Scripts/Assembly-CSharp/{BoosterPackOpenWindow,CardInBoosterPack,BoosterPackBackground}.cs`（方法体被 ILSpy 剥离，签名/字段权威）
- 现项目：`d:/warpforge/scripts/packs.gd`（`_show_opening_animation` L449 / `_on_slot_click` L502 / `_finish_open` L542）
