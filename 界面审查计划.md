# 界面完全彻底审查计划（2026-08-22 用户指定）

> 状态：**2026-08-23 6 组审查全部完成**（报告 `d:/2/UI审查结果_{A,B,C,D,E}_0823.md`+权威表 `d:/2/audit_*_*.md`）；批次 1-3 修复 18 文件完成（字号/重叠/黄钮/卡面高清/圆钮等，见 项目任务文件.md 续 11 行）；**剩余=批次 4 结构性大改**（settings scale 容器/rewards 任务中心重构 P0/ranked 重排/quests-campaign-achievements 4-Tab 竖栏/文案回填 16 处/D-E 余项——清单见任务文件"〇、下一步任务" 0-10 条，F 组（对战/战场）按用户指示暂停）
> 背景：2026-08-21 多轮审查（位置/结构功能/卡组专项）后，用户实测仍发现：导航栏缺失/被盖、界面排版与原版不同、贴图图标用错、卡牌精细度不足（模糊、非"卡框+卡图"组合）。根因：此前审查依赖整理文档（有系统性坐标偏差）且未做视觉/贴图/精细度检查。**本计划为"完全彻底审查"：全部以原始 Unity JSON + chain_rect 权威验证，8 维度全面检查。**

## 一、审查代理（已建，新会话可直接用）

- **`D:\2\.claude\agents\ui-spec-auditor.md`** — UI 全面审查代理（8 维度：说明书符合度/排版有序性/位置尺寸/区域划分/功能/多余元素/画面精细度/卡牌精细度）
- 用法：新会话 `Agent` 工具 → subagent_type 选 `ui-spec-auditor`，prompt 里指定负责的界面清单
- 派代理时按组分配（每组 5-7 界面，文件不重叠），每组 prompt 注明："你是 ui-spec-auditor，负责以下界面：<列表>，按你的 8 维度全面审查，输出格式按你的报告模板"

## 二、审查范围（全部 35 界面 + 弹窗，分 6 组）

| 组 | 界面 | 备注 |
|---|---|---|
| A 全局 | main_menu / card_displayer / player_profile / choose_name / settings / inbox | 主菜单+顶栏+档案+设置 |
| B 卡组 | deck_collection / deck_builder / deck_info_popup / import_deck_popup / mode_select / ranked | 卡组链（8.21 已修一轮，需复核） |
| C 收藏商店 | collection / cosmetics / where_cards_popup / shop / offer_popup / packs / booster_info_popup | |
| D 任务活动 | rewards / quests / campaign / achievements / daily_streak_popup / base_event_popup / two_sides_event | |
| E 社交其他 | social / give_feedback_popup / rate_popup / draft / draft_expiring_popup / gacha / forge / tutorial | |
| F 对战 | battle（HUD/手牌/战场/结算/教程引导）+ 12 阵营 arena | 对战视觉最大模块 |

弹窗类（deck_info/import_deck/offer/booster_info/daily_streak/base_event/rate/give_feedback/draft_expiring/where_cards）重点查：窗口位置尺寸/内部元素/关闭钮跨窗顶设计。

## 三、审查方法（每界面固定流程）

1. `dump_go_tree.py` 提取原始 JSON 子树（定位用菜单全树.md 找根 GO 名）
2. 逐元素 `chain_rect.py` 权威换算（位置/尺寸/锚点）→ 与项目 .gd 代码逐项比对
3. **贴图核查**：m_Sprite PathID → Sprite JSON m_Name → 项目 load 路径对照（用错/近似替代/缺失列出）
4. **文字核查**：m_text 对照项目 Label 文本（UI 文字用 JSON 英文）
5. **功能核查**：读 .gd 的信号连接/数据源（真实 vs 写死）
6. **精细度核查**：贴图分辨率 vs 显示尺寸、卡牌"卡框+卡图"组合方式（读 Sprite m_Rect 裁边/m_Pivot + 卡框贴图）
7. **导航核查**：左侧 x0-166 导航可见性（NavBuilder 在 _ready 末尾/独立窗口例外）
8. 输出结构化报告（严重度 P0/P1/P2 + chain_rect 依据 + 修复建议）

## 四、重点专项（用户明确指出）

1. **导航体系**：全界面左侧导航（8.21 已修 20 界面+rewards Tab 栏，需全量复核无遗漏；deck_builder 编辑器页例外=无导航）
2. **"本界面没有的图标/自作主张排版"**：每个界面列出说明书树里没有的元素（自创标题/自创按钮/放错界面的元素）——收集成"多余元素总表"
3. **画面精细度**：所有界面贴图分辨率核查——原版 Sprite JSON m_Rect 原始尺寸 vs 项目显示尺寸，拉伸放大>2x 的列"模糊/不精致"清单；按钮无底（flat=true）清单
4. **卡牌精细度专项**（用户：卡牌模糊/不够高清，原版应该是卡框+卡图）：
   - 查原版卡牌显示结构：Card Frame（卡框贴图，如 12_主程序资源 或 01_卡牌 的卡框）+ Card Art（卡图）是否分离组件
   - 项目显示方式：贴图路径/缩放/滤镜（texture_filter）/是否用 thumb 小图放大
   - 卡图分辨率 vs 显示尺寸（如 660×1024 立绘放大到多少显示）
   - 修复方向：用原始大图（faces 而非 thumbs）、卡框+卡图分层、texture_filter 默认 linear
5. **区域划分**：ScrollRect 掩码、元素溢出/重叠清单

## 五、输出与验收

- 每组审查产出：`D:\2\UI审查结果_<组名>_<日期>.md`（结构化报告）
- 主代理汇总：按严重度合并 → P0/P1 先修 → 修复后 chain_rect 复核 → 用户 F5 目视验收
- 验收标准：每个界面 = 说明书逐项对照（位置/尺寸/贴图/文字/功能/精细度），"基本达标"不构成完成

## 六、后续迭代

- 审查发现的通用问题（如某类贴图系统性用错）→ 写入 解包资源使用地图.md 坑清单
- 审查代理定义随经验迭代（.claude/agents/ui-spec-auditor.md 可直接修改）
