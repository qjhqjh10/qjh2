# Warpforge 项目任务文件（活任务清单）

> 更新：2026-08-20（第十四轮续28：**EC 规则批次完成**——Ecstasy/Stimulation/Dark Pact 契约全接线；可选低优先+规则缺口全部处理完毕）；任务清单已瘦身——历史轮次归档，见"〇、下一步任务"）
> 本文件用途：**新会话确定"接下来做什么"**。完成项打钩；历史轮次详情已从本文件删除（git 历史可查），不再保留流水账。
> 给新会话的情况说明见文末。

---

## 〇、下一步任务（新会话从这开始）

**当前状态（2026-08-20 十四轮续28 收尾）：原版 13 模块全部完成；P1 遗留已清；P2 批量修复 28/38 项；攻击类 VFX 276 全转；Vargard 替身；[Energy]/[Faith] 激活三货币；Faith/Might/Pray/choose-one；**EC 规则批次（Ecstasy 9 卡攻击计数/Stimulation 9 卡打出触发/Dark Pact 契约 36 卡全链路）**；community_cards 补卡 14 张；**可选低优先+规则缺口全部处理完毕**；全回归绿（rule_test 259/0）**

**接下来（按优先级）：**

1. **目视验收（P1，需用户运行游戏参与）**：逐界面检查（重点：主菜单模式卡标题/计时行、settings 5 页、tutorial 六关+Play 按钮、forge 石柱、cosmetics 网格、deck_info Switch 统计视图、结算奖励条、手牌 180×285、3D 特效、[Energy]/[Faith] 激活弹窗、Pray 技能按钮、choose-one 弹窗）→ 发现问题用 chain_rect/原始 JSON 对照修复 → tools_dev/shot_*.gd 整屏截图 + vision 复验
2. **问题卡核查（P1，用户）**：`D:\2\卡牌问题检查\` 80 张（75 号已确认 = Vico Therbeus 卡面并记录；其余待用户逐张确认，发现同类型归属问题按 75 号模式记录到 说明.txt）
3. **已处理项（记录，非待办）**：~~攻击类 VFX~~/~~Vargard 替身~~/~~[Energy]+[Faith] 激活~~/~~Might/Pray/choose-one~~/~~community_cards 14 卡~~/~~OBJ UV~~/~~Ecstasy 9 卡~~/~~Stimulation 9 卡~~/~~Dark Pact 契约 36 卡~~ **全部完成**；**Deck Counter Drawer**（说明书 m_IsActive=false 默认隐藏+反编译 stub 无激活依据→不做）；**FlavourTextBG**（lore 无数据源）；**Nork Deddog Talent**（效果文本缺失）；**AnimationClip 44 个**（环境动画已覆盖）；**小量关键词**（Frontline×1/Deep Strike×1/Lord of the Storm×3/Genomic×2——效果由 desc 解析部分生效，专有触发点无原版语义数据）
4. **已分析不做的 P2 项**（详见 git 历史 8.31 节 ⏭️ 标注）：主菜单卡纵位 y182（几何冲突）/邮件挑战 x+27（与卡片区重叠）/聊天预览条（chain_rect 验证当前正确）、player_profile 7 项（8.23 验收定稿冲突）、ranked Division Info 重排（JSON 预布局不可靠）、quests 任务图标（解包仅 2 个）/周常条 x70（无依据）、gacha 坐标 3 项（bundle 未导出）、deck_collection Create New Deck 空槽（无 JSON 证据）、card_displayer Swap Style y632.7（布局冲突）、battle 4 项（8.19/8.24 已定）、two_sides 默认页（无 team 数据）、settings Tab y139.3（说明书容器）——**均不做**

**持续流程**：每任务后更新 解包资源使用地图.md（新资源用途/踩坑）+ 解包资源列表清单.md（使用状态，目标：全部资源用上=项目完成）

## 一、项目背景

- 战锤 40K Warpforge（已关服卡牌游戏）解包资源 → **Godot 4.7.1 单机复刻**
- 游戏项目：`D:\warpforge`（场景 scenes/ + 脚本 scripts/ + 工具 tools_dev/）；运行 exe：`D:\godot\Godot_v4.7.1-stable_win64.exe`
- 解包资源：`D:\2\解包整理\`（只读源，44.9 万文件）；反编译代码：`D:\2\Warpforge_code\`（查字段/类权威）
- Python 工具：`D:\2\Warpforge_tools\py312\python.exe`（勿用系统 Python）
- git 仓库：**D:\2**（唯一远程 origin/master，推 GitHub qjhqjh10/qjh2）；游戏代码不入库，靠 `D:\2\Warpforge备份\` 备份
- 数据管线：`D:\warpforge\data\`（card_stats.json 1106/1193 卡有 OCR 数值 + card_ids.json 996 映射 + decklists 236 套）

## 二、必读文件（新会话先读）

1. `D:\2\解包资源使用地图.md` — 资源用途/在用状态/缺失清单/使用中的坑（**UI/资源任务必读**）
2. `D:\2\原版游戏结构.md` — 复刻蓝图（原版 13 模块 + 实施顺序）
3. `D:\2\项目资源总览.md` — 顶层目录地图
4. **说明书**（做 UI/界面任务必查）：`Warpforge_tools/data/ui_layout/菜单全树.md`（313 根界面）+ `菜单全树_要点_第{1-4}块.md`（每界面元素表+未实现清单）+ `battlearena1全树.md`（对战 HUD）+ `卡组界面说明书.md`；原始 JSON 在 `解包整理/03_界面UI/`、`07_场景/`
5. `D:\2\解包资源列表清单.md` — 每任务后核查更新（目标：全部资源用上=项目完成）

## 三、最高准则（用户反复强调，违反=返工）

1. **UI 100% 按说明书执行**：**"说明文件"= 原始 Unity JSON（`解包整理/03_界面UI/菜单/`、`07_场景/` 下的 GameObject/RectTransform/MonoBehaviour/Sprite 文件，非整理文档）**——完整写明一切（Image m_Sprite/m_Color、Text m_text、RectTransform 坐标尺寸锚点、Sprite m_Rect/m_Pivot）。**代码编写/修改/审查一律直接检查原始 Unity JSON**，坐标/尺寸/贴图/文字数值直接照抄（m_AnchoredPosition/m_SizeDelta/m_AnchorMin/Max/m_Pivot/m_Sprite/m_text/m_Color）；**禁止自设坐标/拍脑袋排布**；"基本达标/无明显错误"不构成完成，界面不美观=未按 JSON 布置=代码审核。**分辨率已查证（2026-08-19）**：原版 CanvasScaler m_ReferenceResolution=1920×1080（ScaleWithScreenSize 匹配宽度，`07_场景/battlearena1/MonoBehaviour/MonoBehaviour_4731.json`），项目 viewport 同为 1920×1080 → **JSON 坐标数值可直接使用，无需换算**。整理文档（菜单全树.md/卡组界面说明书.md/要点文档）仅作定位索引，与原始 JSON 冲突时以原始 JSON 为准并更正整理文档。
1.5. **逐界面 JSON 审查流程（用户指定 2026-08-19）**：审查任何界面 = ① 先读该界面原始 Unity JSON（dump_go_tree.py 按 GO 名 dump 子树）→ ② 逐个元素找项目对应代码（tscn/gd）→ ③ 逐项比对（坐标/尺寸/锚点/贴图/文字/active/功能/特效/铺设完整性/区域划分）→ ④ 修复 → ⑤ 复验。审查不止坐标：**功能作用**（按钮事件）、**特效与交互反馈**（hover/点击/进出场动画）、**铺设完整性**（该铺的纹理/底图/图标/视频有没有铺）、**区域划分**（掩码裁剪区/交互区）。
2. **视频/截图分析暂停**（用户没额度，直到告知恢复）：问题 = 找说明书 → 与代码对比 → 按说明书修改；验收以"对照说明书预期"为主；vision 仅在有额度时做整屏截图验收。
3. **黑色必须有说明书依据**（用户 2026-08-19）：弹窗遮罩黑有依据=原版 Menu Dark Background（0.77 alpha 全屏底，实现统一用暗红 Color(0.11,0.045,0.055,X) 防纯黑死区）；无依据的黑（如顶部装饰条）一律不加。
4. **复刻一致性**：改动前先查解包资源+说明书 → 按说明书复刻 → 缺失才替代（**imagegen skill 生成**，禁止视频帧/截图裁剪）→ 自查三问（素材名/位置尺寸/质感）→ 验收。
5. 解包资源优先；改动前备份 → 测试全绿 → 删备份；探索发现及时落档（使用地图/本文件）。

## 四、项目组成与完成清单（原版 13 模块打钩）

| 模块 | 状态 | 关键产出 |
|---|---|---|
| 全局框架（导航/顶栏/货币/Toast/Loading/音乐音效） | ✅ | nav_builder.gd 5 键 / menu_bg.gd / toast.gd / loading.gd / sfx.gd |
| 主菜单（大厅） | ✅ | main_menu.tscn + card_displayer（制作/升级/通配符）+ player_profile 6 标签 + settings 5 页 + choose_name + 聊天面板 |
| 模式选择 | ✅ | mode_select（练习/多人，卡组列表+督军立绘+法力曲线）+ ranked（段位+排行榜弹窗+Ranked Deck Selection） |
| 卡组 | ✅ | deck_collection + deck_builder（Sidebar/筛选栏/督军行/稀有度限制 30 张）+ deck_info_popup + import_deck_popup |
| 收藏/图鉴 | ✅ | collection（搜索/稀有度/费用/阵营/类型筛选 + 详情 + 获取途径弹窗）+ cosmetics 卡背 |
| 商店/经济 | ✅ | shop 3 标签（卡包/美容品/资源）+ offer_popup + 金币持久化 |
| 卡包/开包 | ✅ | packs（翻卡动画+稀有度权重+通配符闭环）+ **保底系统**（每包+1 出传说重置 200 阈值）+ booster_info_popup |
| 任务/奖励/战役 | ✅ | rewards（每日 7 天）+ quests（3 任务+**周常条**）+ campaign（轨道 15 节点）+ achievements + daily_streak + base_event + two_sides |
| 社交/联盟 | ✅ | social 双 Tab（公会成员列表+5 级宝箱得分 / 好友） |
| 设置 | ✅ | settings 5 页（General/Media/Account/Graphics/Support，Support 含反馈问卷+评分） |
| 对战 | ✅ 完成 | battle + 3D 战场（12 阵营环境装饰 OBJ+图集+31 场景粒子）+ BattleDoors + 拖拽部署 + 语音 + 事件特效音效 + 卡牌动画 VFX + 结算视频 + 教程逐条引导；全部按说明书 |
| 教程 | ✅ | tutorial（6 关列表 + 对战内**原版 turnScriptedData 逐条引导**（79 步提示/玩家动作要求）） |
| 特效/音频扩展 | ✅ | unity_particles.gd 18 粒子（攻击/爆炸/召唤/治疗/眩晕/增益/护盾/卡牌动画 VFX）+ 28 战斗音效 + 12 阵营 3D 场景粒子 |

## 五、历史轮次（已归档）

> 8.1-8.31 详细轮次记录已删除（git 历史可查：`git log -p 项目任务文件.md`）。所有遗留项均已解决或移入"〇、下一步任务"；技术细节/坐标/踩坑已落档 `解包资源使用地图.md`（坑 1-32）与 `Warpforge备份/`（*.bak_* 备份）。

## 六、验证命令（改动后必跑）

- UI 改动回归：`godot --headless --path D:/warpforge res://tools_dev/auto_test.tscn`（0 错）
- 界面专项测试：`test_collection_filters / test_quests / test_campaign / test_achievements / test_popups / test_player_profile / test_two_sides`（.tscn，headless，输出 PASS/FAIL）
- 战斗专项：`test_battle_hud`（门动画/拖拽校验）/ `test_battle_hand`（手牌显示/贴底/数条说明书尺寸）/ `test_arena_props`（12 阵营环境装饰 OBJ+粒子数量/材质）/ `test_tutorial`（教程逐条引导提示切换）/ `test_card_vfx`（卡牌动画 VFX 接线+攻击类抽查 26 个）
- 卡组链路：`godot --headless --path . --script res://tools_dev/e2e_deck.gd`（组建→收藏→选卡→battle 全过）
- 对战：`--script res://tools_dev/battle_sim.gd`（随机性，跑 2-3 次）
- 截图：`tools_dev/shot_*.gd`（**整屏 1920x1080**，禁止局部裁图）
- 新资源同步后：`godot --headless --import`（OBJ/纹理都必须 import）

## 七、核心踩坑速查（详细见使用地图）

- **GDScript**：`or` 返回 bool（`int(x or 0)` 全变 1，必须 null 判断）；typed Dictionary 赋值 null 报错；`:=` 从 Variant 推断报 Parse Error（脚本加载失败→headless 进程空转，用 timeout+stderr 诊断）；int(null) 报错
- **queue_free 延迟生效**（测试需等 2 帧）；_ready 期间 add_child 报 "Parent node is busy"→call_deferred
- **Button.flat=true 时 StyleBoxTexture override 完全不渲染**（2026-08-20 实测——按钮底图必须 flat=false+4 态 override，或 flat=true+StyleBoxEmpty+子 TextureRect；坑 32）；**底图当 icon 会图标+文字并排分离**→子 TextureRect+Label 最后加
- **GPUParticles2D 默认 emitting=true**（入树前关）；ParticleProcessMaterial 是 3D 语义（velocity float/gravity Vector3/scale_min）
- 解包纹理预乘 alpha 损坏需修复；**新资源必须 --import** 否则 load null；Godot load() 不支持绝对路径（用 Image.load）
- set_anchors_preset(CENTER) 不居中→手动算 offset；set_anchors_preset(TOP_LEFT) 后负 offset 失效→FULL_RECT
- user:// 路径 = AppData/Roaming/Godot/app_userdata/Warpforge 卡牌游戏/（profile.json 金币/通配符/owned/quests/campaign/draft/gacha/forge/streak/two_sides/rating/feedback）
- 阵营图标文件名别名（GameData.army_icon_path 已封装：UM=Ultramarines/Goff→Orks/SpaceWolves 特判）
- **headless viewport 是 1920x1920（非 1080）**——布局绝对坐标断言在 headless 下失真，用相对布局判断（子节点底部 ≈ 容器底部）
- **OBJ 模型是 Unity 归一化小模型**（顶点范围 ~0.02）→ 放大 500-800 倍使用（**泰伦例外：真实比例**，放大 1-20 倍）；06_模型 各阵营 OBJ 无 mtl（材质在 Unity 侧），用纹理图集或单色金属材质；**平躺 OBJ（y 薄、z 长）需 rot_x +90 立起**（原版场景根旋转 -90°X 补偿；Godot 默认欧拉 YXZ=先本地 X 站立再世界 Y 朝向）
- **场景 bundle Mesh 可能在 battlesharedresources 共享包**（MeshFilter m_FileID≠0 → 按 pid 全 bundle 扫）；**UnityPy 1.24 `o.export()` 是 None**（ObjectReader.__getattr__ 委托返回 None，必须 `o.read().export()`）；**typetree m_TexEnvs 是 tuple 对**（JSON 导出才是 list 对，isinstance 判断须含 tuple）；材质引用本地 Material JSON 优先（解包已重映射），miss 再查共享包
- **场景世界坐标基准**：所有阵营 BoardCamera 统一 (100,2.22,-13.57)，粒子/装饰原版坐标换算 = 减 (100,0,0)；**GPUParticles3D 材质类名是 ParticleProcessMaterial**（无 3D 后缀）；新脚本 class_name 需 --import 注册
- **大 Edit 替换时注意函数边界**（本会话踩坑：3D 卡牌初始化段落进 _add_prop 导致重复执行+数组越界——改完跑测试抓出）
- 卡牌数值来自 **OCR 卡面识别**（原版数值在服务器下发，解包无数值——CardData.cs 只有结构，全解包 0 个数值型 health）

## 八、给新会话的情况说明

【2026-08-20 十四轮续28 交接（EC 规则批次：Ecstasy/Stimulation/Dark Pact 契约）】① **Ecstasy N（狂喜，9 卡）**：EC 完美主义核心——单位累计攻击 N 次后触发段落（attacks_total 跨回合计数+触发重置；`_parse_desc_fx` 支持 "Ecstasy N:" 格式，数字存 ecstasy_n→kws["ecstasy"]=N；declare_attack 计数处 _trigger_fx）；接线 Tormentor(1)/Icon of Excess(2)/Malgarash(2)/Terminator/Threnodic Choir(2)/Maulerfiend(5)/Xarahan(15 近似跳过) ② **Stimulation（激励，9 卡）**：打出任意卡时场上有激励单位触发（反编译 triggerCardPlayedByStimulation 证据；play_card 扣费后 _check_stimulation 广播）；Malgarash/Glittering/Infractor Obsessionist 等 ③ **Dark Pact（契约，36 卡）**：4 契约定义 DARK_PACT_FX（Fate+2血+潜行/Blood+2近战+先锋/Excess+2攻+2远/Resilience+1血+再生）；`_grant_dark_pact`（记录 kws.darkpact + 拆 "and" 直接 _apply_gain（give 解析器需 to 目标且多属性只取首个）+ 收到契约触发链：自身 "After receiving"（Aspiring/Chaos Legionary/Meltagun）+ Havoc 广播）；解析点：Gain（Chosen Rally/Strike）/Give（Dark Apostle Artifice）/Chaos Lord 部署广播/Khorne Berzerker 死亡广播（→Blood）；_apply_gain 补 ranged attack ④ **验收**：rule_test 新增 _test_ec_mechanics（+9 断言）→ **259/0**；全回归绿（auto_test/battle_sim×2/e2e/6 专项）⑤ **踩坑（写进使用地图）**：Edit 误删函数体 4 次（整函数 old_string+签名 new_string 模式——恢复后 grep 计数验证）；python heredoc `\`≠文件 `\` 续行符；_apply_gain 前缀匹配大小写敏感（契约原文大写需 to_lower）；str.split 从 Variant 推断失败（显式 : Array）；replace 锚子串误伤函数定义行 ⑥ 备份 rule_core.gd.bak_ec_0820+battle.gd.bak_ec_0820 ⑦ **规则侧可复刻项至此全部完成**（攻击 VFX/替身/[Energy]+[Faith]/Faith/Might/Pray/choose-one/Ecstasy/Stimulation/Dark Pact/补卡 14）；已评估不做有记录（Deck Counter/FlavourText/Nork/AnimationClip/小量关键词）⑧ 下一步：P1 目视验收（用户参与）+ 问题卡核查（用户）

【核心准则速查】① **UI 100% 按原始 Unity JSON**（03_界面UI/菜单、07_场景 下 GameObject/RectTransform/MonoBehaviour/Sprite；禁止自设坐标；整理文档仅定位索引）② 分辨率 1920×1080 相同 → 数值可直接换算（chain_rect.py 权威：y 翻转/pivot 修正/父链累加/锚点中心；**拉伸锚点已修复**）③ **子代理/整理文档坐标必须 chain_rect 交叉验证**（实测子代理 2 处错误）④ vision 已授权直到额度用光（整屏截图 tools_dev/shot_*.gd；暗场景误判用 PIL 像素采样）⑤ 黑色必须有说明书依据（遮罩暗红 Color(0.11,0.045,0.055,X)）⑥ 缺失资源用 imagegen 生成（禁止截图裁剪）⑦ UI 文字用 JSON 英文（改事件文本必须同步 _fx_from_event 解析+格式串参数序）⑧ **flat=true + StyleBoxTexture 不渲染**——按钮底图必须 flat=false+4 态 override 或 StyleBoxEmpty+子 TextureRect（坑 32）⑨ git push 前先询问用户（网络失败只试一遍）⑩ 完成一项落档任务文件+使用地图+资源清单

