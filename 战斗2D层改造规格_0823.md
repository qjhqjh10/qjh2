# 战斗 2D 层改造规格（撤 3D 换 2D）— 2026-08-23

> 用户裁决：**战斗暂时不要 3D 场景，画面换原版 2D 层**。
> 本文档=只读调研产物（未改任何代码）。依据：`解包整理/说明书/01_战斗_对战/battlearena1_2D层全树.md`（855 行全文，与 `2D层_battlearena1全树.md` 逐字相同，无差异）、`战斗HUD说明.md`、`02_战场_场景/battlearena1.md`（529 行全文）、`d:/warpforge/scripts/battle.gd`（4694 行全文）、`unity_particles.gd`/`unity_particles3d.gd` 全文、`test_arena_props.gd`/`test_card_vfx.gd` 等测试源码。

---

## 0. 结论摘要

1. **原版 2D 层没有背景元素**：`BattleHud/Canvas`（BackCanvas/FrontCanvas）下**全部是 HUD 控件**（玩家信息/能量/牌库/手牌/攻击选择器/聊天/设置/结局层/教程），**没有任何全屏背景 Image/RawImage**。战斗背景 100% 来自 3D `Scenario` 子树（`Scenario Battle Arena 1 Baked` 的 `Back background`/`Background`/`Floor plane` + 粒子 + 相机）。→ **撤 3D 后背景必须用"烘焙静态背景布"补位**（推荐方案 A，见 §3）。
2. **改造规模估计：battle.gd 单文件，约 25 处改动点，净增约 250~350 行**（新增 `use_3d` 开关分支 + `_build_2d_world` + 2D 场上卡渲染 + `_fx2d` + 槽位常量表）。策略=**"3D 函数体全保留，默认用开关停用"**（`var use_3d := false`），测试可 `set("use_3d", true)` 走原 3D 路径 → 破坏面最小、未来恢复 3D 一键切回。
3. **无需动的部分**：全部玩法逻辑（部署/拖拽/攻击/战术/换牌/AI/回合/胜利结算）、全部 2D HUD（信息区/能量/牌库/手牌/日志/设置/坟场/详情弹窗/结局视频）、`scenes/battle.tscn`、`unity_arena_battlearena1.tscn/.gd`（保留不加载）、`unity_particles.gd`。只有**场上单位显示**（现为 3D 卡+Label3D）、**战斗特效挂载**（现 `_fx3d`→GPUParticles3D）、**槽位热区定位**（现靠 `_cam3d` 投影）三项需要 2D 化。
4. **附带收益**：悬而未决的"battlearena1/黑军团 手性方向"问题（使用地图坑 52）在 2D 化后**降级为一层 PNG 的水平镜像**（`ImageOps.mirror` 一行），不再需要 3D 数学。
5. **需要一次性采集的前置数据**：18 个槽位（2 侧×9）在现 3D 投影下的屏幕矩形表（改造前用脚本从当前 3D 版采集硬编码），外加 1 张 1920×1080 场景烘焙背景 PNG（现有工具 `tools_dev/shot_arena_alone.gd` 直接可用）。

---

## 1. 原版 2D 层权威结构表（battlearena1, 说明书全文核对）

说明：以下坐标=说明书已按 chain_rect 换算的 Godot 1920×1080 y 向下绝对坐标；`(inactive)`=场景序列化中 `m_IsActive=false`（默认不显示）；sprite=PathID（对应解包贴图，项目已大量提取到 `assets/ui/battle/`）。

### 1.1 根结构（3 根：Scenario=3D 世界 / PlayFabHttp / BattlePrefab=战斗逻辑+2D UI）

```
BattlePrefab
├─ Scene Initializer / BattleManager / Battle Events Controller / EnvironmentConditionsController
├─ BattleHud ── Canvas [0,1080]
│   ├─ Aspect Ratio Filler Top [-1626,-1482 3252x1842]   ← 超宽屏上下填条(21:9 时延伸; 16:9 不可见)
│   ├─ Aspect Ratio Filler Bottom [-1626,1800 3252x1842]
│   ├─ BackCanvas → Safe area BackCanvas          ← ★ 无任何背景贴图, 全部为控件
│   │   ├─ LeftArea
│   │   │   ├─ CardsPlayedInTurnHolder (inactive) 105x28 — 本回合已打出卡 3×20x20 图标
│   │   │   ├─ PlayerInfo [32,977 260x75]  ← You 信息区(左下)
│   │   │   │   ├─ NameBackground [-38,951 436x126] (UI_Player_Frame)
│   │   │   │   ├─ PlayerNameText [113,977 249x45] 35px 'Player name'
│   │   │   │   └─ Milestones [158,940 174x38]: MatchSkulls Icon 65x54 + 'x3' 35px
│   │   │   ├─ CemeteryLogPanel (inactive) x[-1200,406] 794x654 — 滑入式坟场(四边 logframe 拼框 + 10 SliderUI)
│   │   │   └─ Left Anchor: CenterCameraButton [18,1108 64x62] / OffensiveButton [0,987 109x107]
│   │   ├─ RightArea
│   │   │   ├─ SettingsBtn 64x64 (UI_Settings_Icon)
│   │   │   └─ Right Anchor:
│   │   │       ├─ Energy And turn holder [235,785 239x357]
│   │   │       │   ├─ Background 302x481 (UI_Energy_Holder_big; 右缘出屏 160px=原版设计)
│   │   │       │   ├─ Clock 134x86 → TurnBtn 131x80 'END TURN' 31px (三态宽版贴图)
│   │   │       │   ├─ EnemyMana 75x78+98x98 QP holder(+SpiritStone (inactive)) + Energy Accumulation VFX On
│   │   │       │   └─ PlayerMana 76x77(+Faith (inactive)): Energy Player 91x93 + ManaText '10/10' 40px
│   │   │       │       + Energy Accumulation VFX Off
│   │   │       ├─ OvertimeIndicator 69x71 (40k_icon_overtime)
│   │   │       ├─ PlayerDeck 230x230 (UI_Deck_Background) + NotYourTurnImage 24x36 + 'Cards left: 2/5' 42px
│   │   │       ├─ EnemyDeck 200x200（敌方, 初始屏外顶部滑入）
│   │   │       └─ ReplayButtons 294x57: Replay/Play/Pause(inactive)/StepPlay 各 80x49
│   │   ├─ HandLimitArea 1x1
│   │   ├─ UpperAnchor: EnemyArea 100x100 (敌方手牌区, 顶部中央) + EnemyAssetArea
│   │   └─ BottomAnchor: PlayerArea → HandArea → (CardsInHandText (inactive) 288x90 'Cards left: XX'
│   │       + HandAnchor) + PlayerAssetArea; PlayerCardAreaSizeHelper To Use [-50,830 100x250]
│   │   └─ WaitText (inactive) 1344x79 + Dark Shade
│   ├─ FrontCanvas → Safe area FrontCanvas
│   │   ├─ ShadePanel (inactive)
│   │   ├─ Drag Attack Selector 622x122: Select Attack Background(inactive) 780x285 @(570,497)
│   │   │   + Melee/Range/ActiveSkill 三钮 118x119 同位重叠单显 (+Highlight 176x177, ValueText '0')
│   │   ├─ ActiveSkillDesc (inactive): NameText 'Fire Arrow' / CostText '10' / DescText / '0 available'
│   │   ├─ Unit Chat: PlayerChatDisplay 649x237 (Mask 144x157+CardImage+ChatText) / EnemyChatDisplay + wave 388x63
│   │   │   + ChatPopup(inactive) / RadioChat(inactive)
│   │   ├─ Mulligan: MulliganText 1344x79 ('Choose cards to replace in first hand' 65px + 'You go second')
│   │   ├─ Generic Multi Card Display Combat (inactive): Header Text + Viewport/Content
│   │   │   + CardUI Reference（2DCard 完整模板）+ BattleContinueButton 833x50
│   │   ├─ ChooseCardMenu (inactive): HideChooseButton 78x78 + Continue 548x76
│   │   ├─ Card Display Window (inactive): Card Display 752x868 @(584,106)
│   │   │   （LowerSection + FlavourTextBG 1320x178 + Voice Over 89x89 + Cards→CardUI×5 全 2DCard 模板:
│   │   │    UI Collider / Front(Card Highlight And Shadow 4x4 / CardImage / CardFrame /
│   │   │    Card Info(Health '30'/Armour '2'/Range '12'/Melee '5'/Cost '1' + 底 + Rarity 宝石 +
│   │   │    Name and description(NameTextUnit/DescTextUnit/RaceText 'Troop')) / EffectAnchor /
│   │   │    Cardback Container(inactive) / New Card Badge / Ban Icon(inactive)）
│   │   │   + TutorialObjs（MeleeText/RangedText/HealthText 380x55 + Arrow 206x76 + EnergyText）
│   │   ├─ DebugButtons (inactive) — 原版调试面板(Player/Enemy debug buttons/Card Picker)
│   │   ├─ AboveShader: OvertimeSplashText (inactive) 'OVERTIME!' + 181x187 Image
│   │   ├─ PopupAnchor
│   │   ├─ BattleSettingsPanel (inactive) 743x759 @(588,190): Generic Popup Background 739x761
│   │   │   + Generic Close Button 75x75 + Auto Zoom Toggle 472x76('Auto zoom') + Debug Buttons(Win/Draw/Console 249x100)
│   │   │   + Volume Sliders( Music/Sound Effects/Voice-overs 三滑杆 12px + Text 55px )
│   │   │   + ChatToggle 181x57 ON/OFF('Mute opponent') + Resign 300x90 + SkipTutorial 300x90 + Language Selector
│   │   ├─ Tutorial 1920x1080: TutorialTip 430x150 @(749,466) + InitTutorialTip(inactive) + TutorialArrows(inactive)
│   │   │   + TutorialPointerCombat(inactive: Pointer 100x100 + light)
│   │   ├─ LoadingMenu (Panel inactive)
│   │   └─ Alliance Panel (inactive) 815x475 — EnemyInfo 260x75 + Alliance 552x136 + 徽章
│   └─ TooltipManager
├─ BattleDoors (inactive) [-960,526 1920x1119] — 满屏结算层: EndBattlePanel(Background 3840x2160 + Video Image
│   1920x1080 + SkullsHolder 648x52(3 骷髅 64x71) + RewardsHolder 495x49('2000') + HolderRating(Trophy 60²+'34'))
│   + Tutorial highlight(inactive) + Anim Anchors×5 + UI Error Message Controller
│   （项目已按 8.13 结论把 BattleDoors 判为"结算层 inactive, 无开场门动画"→ 无对应实现, 正确）
├─ BattleTipController / BattleBoardElements（3D: Colliders/BoardCamera/Cinemachine/PlayerBoardArea/
│   EnemyBoardArea→MinionArea→LeftMinionArea(inactive)/Particle colliders/Board Center/TapParticleController）
├─ NoCanvas2D: Attack Target Reticle + Crosshair(inactive) + CrosshairLine 3D(inactive)
├─ TouchInputManager / Battle Music Controller / TutorialController / CardMaterialHelper / BattleCacheManager 等
```

**关键结论（背景方案的事实依据）**：
- `BackCanvas`/`FrontCanvas` 下**没有任何全屏背景 Image**；全屏元素只有：(a) 两片 Aspect Ratio Filler（填条，16:9 时在屏外）、(b) 结算层 `BattleDoors`/`EndBattlePanel`（`m_IsActive=false`）、(c) `Tutorial` 层与各 inactive 面板的遮罩/底。
- 画面背景在 3D 根 `Scenario`：`Scenario Battle Arena 1 Baked`（世界(0,-0.1,-3.34)）挂 `Back background`（贴图 `Battle Arena 1 Back  Background.png`）/ `Background`（y15.8, `Battle Arena 1 Background.png`）/ `Floor plane`（`Battle Arena 1 Floor.png`，emission 0.19 全发光）/ 全部道具网格（同 `BattleArena1 Texture Baked.png`）+ 34 粒子 + Sun flare + Directional Light + BoardCamera（FOV 46.397, lensShift y=-0.205, pos(100,2.222,-13.572)）+ Env（Trilight→已是 SH/环境色 0.289,0.183,-0.08 + Linear + LUT 后处理）。
- `CanvasScaler` 1920×1080 匹配宽度；UI Camera 正交挂 BattleHud。

### 1.2 原版 2D 卡（2DCard 模板）结构（Card Display Window → CardUI → 2DCard 权威模板）

```
2DCard
├─ UI Collider
├─ Front
│   ├─ Card Highlight And Shadow 4x4 (SDF 帧; 高亮色随状态)
│   ├─ CardImage (立绘, 内窗 495×813 of 1024²)
│   ├─ Textbackgrounds (inactive) — TextBackground Big/Small UI
│   ├─ CardFrame (阵营兵种 tier 帧)
│   ├─ Card Info（每项 = 容器 0x0 绝对小锚 + 文字 30px 级; 实时数值）
│   │   ├─ Health Container '30' / Armour Container (Image + '2') / Range '12' / Melee '5'
│   │   ├─ Cost Container: Cost Background(能量图标 91x93) + CostText '1'
│   │   ├─ Cost Container Big (inactive)
│   │   ├─ Rarity: Card Rarity Sprite（稀有度宝石）
│   │   └─ Name and description (MinionOrWarlord Container(inactive) / Tactic Container)
│   │       NameTextUnit 30px / DescTextUnit / RaceText 'Troop' / ArmyTextUnit(inactive)
│   └─ EffectAnchor
├─ Cardback Container (inactive): 卡背 Shadow SDF + 卡背
├─ Card Ready for level up (inactive) / New Card Badge ('Новинка!') / Ban Icon (inactive 'Запрещено')
```

项目已把此模板换算为常量（battle.gd L35-50 `CARD2D_KX/KY/CARD_ART_W/H/CARD_GEM_POS/CARD_NAME_Y/CARD_DESC_Y`），2D 场上卡直接复用。

---

## 2. 项目现状：保留 / 需改 / 需删 清单（文件:行号）

### 2.1 3D 依赖调用关系图（battle.gd）

```
_build_ui:1664
├─ _build_3d_world:380 ──(1673)──  load scenes/unity_arena_battlearena1.tscn:386 → _cam3d:389(找不到才 new:391)
│      │                              ├─ _build_arena_props:398 → _arena_props_root:519-522
│      │                              └─ 2×9 卡 holder 构建:400-498（front/back/art/frame/gem/hl/name/desc
│      │                                   + 4×Label3D 数值:437-454; _make_card_plane:879）
│      └─ _spawn_scene_particles:504(1675) — 2D 粒子(能量光), ★保留
│
├─ _start_battle:255 ── 290-296 ──（已注释）_arena_floor_tex:348 / _set_arena_props:526
│
├─ _refresh_all:2326 ── _update_3d_cards:991 (2333)   ← 3D 卡刷新(位置/分层/数值/高亮)
├─ _set_mode:4093 ── _update_3d_cards (4103)
├─ _refresh_board:2647 ── _slot_screen_rect:862 (2655) ← 3D 投影矩形 → 槽位按钮位置/尺寸
├─ _center_camera:2342 ── _cam3d position/rotation (2344-2345) ← cam_btn:2039
├─ _screen_to_floor:837 ── 仅诊断工具(shot_3d_diag.gd:41)
├─ _do_attack:3629/3632 ── _fx3d("AttackHitSmall"/"Actual_Explosion", _slot_world_pos:852)
├─ _auto_play:3966/3968 ── 同上(AI)
├─ _fx_from_event:4342 ── 12 处 _fx3d 调用:4368,4370,4395,4403,4422,4425,4428,4433,4454,4483,4489,4492,4523
│      （部署召唤圈/卡牌专属动画/治疗/晕/盾甲/斜射/攻击命中/疲劳）
│      + _find_unit_world_pos:4625 + _show_damage_number:4639(4642 _cam3d.unproject_position)
├─ _fx3d:4618 ── UnityParticles3D.spawn_fx(name, _world3d, wpos):4621
└─ _build_arena_props 子树（use_3d 路径保留）: _set_arena_props:526 → _arena_props_row:761 /
      _add_neutral_props:730 / _arena_particles:795 → UnityParticles3D.spawn:797 / _add_prop:804;
      ARENA_PARTICLES 常量:774（12 阵营）; ☆目前 _start_battle 中已注释不调用(291-295), 仅 test_arena_props 用
```

### 2.2 保留清单（不改）

| 项 | 位置 | 说明 |
|---|---|---|
| `scenes/battle.tscn` | 11 行 | 纯 Control+script，UI 全代码生成，不动 |
| 全部玩法逻辑 | battle.gd `_start_battle`/`_on_hand_pressed`/`_do_play`/`_do_attack`/`_try_select_attacker`/`_on_slot_pressed`/`_auto_play`/`_on_end_turn`/`_check_pending_fx`/`_check_cost_act`/`_check_choose_one` | 纯 2D 状态机，零 3D 依赖 |
| 手牌全链 | `_refresh_hand`:2743 / `_layout_hand`:2850 / `_hand_drag_data`:2877 / `_slot_can_drop`:2905 / `_slot_drop`:2925 / `_on_hand_hover`:2934 | 已 2D（按钮 180×285 弧形布局） |
| 换牌 Mulligan | 1087-1239 | 已 2D |
| 坟场/详情/设置/结算 | 1246-1383 / 1397-1517 / 2352-2464 / 2974-3210 | 已 2D（弹层/视频/统计） |
| 教程提示 | `_update_tutorial_hint`:2268-2323 | 2D 提示条（749,466 430×150） |
| 全部 HUD 构建 | `_build_ui` 1676 之后全部（敌方/You/能量/牌库/按钮/语音/时钟 1723-2080） | 已 2D 绝对定位（说明书兑付完成） |
| `_spawn_scene_particles`:504 | 2D UnityParticles 能量光效 | 保留 |
| `unity_particles.gd`（2D） | 全文 115 行 | 保留为 2D 特效加载器（data/particles/*.json 现 56 个） |
| `unity_particles3d.gd` + data/particles3d/* | 334 数据 | 保留（use_3d 测试路径/未来恢复） |
| `scenes/unity_arena_battlearena1.tscn/.gd` | 2267 行生成物 | 保留不实例化（勿手改生成物——只改生成器） |
| `_set_arena_props`/`_add_prop`/`_add_neutral_props`/`_arena_props_row`/`_arena_particles`/`ARENA_PARTICLES`/`_arena_floor_tex`/`_screen_to_floor` | 526-833 / 348-369 / 837-847 | 函数体全保留（test_arena_props 继续用；`_start_battle` 291-295 已注释掉不用） |
| 字段常量 | `CARD3D_W/H`/`CARD2D_KX/KY`/`CARD_ART_W/H`/`CARD_GEM_*`/`CARD_NAME_Y`/`CARD_DESC_Y`/`SLOT_W/H`/`BOARD_*` | 供 2D 卡复用/参考 |
| 全部入口 | mode_select:1204 / ranked:677 / social:638 / tutorial:206 / draft:807 / deck_info_popup:563 → battle.tscn | 不动 |

### 2.3 需改清单（≈25 处，均 battle.gd + 2 个测试 + 2 个新资源）

| # | 位置 | 改什么 | 为什么 |
|---|---|---|---|
| 1 | L87 附近（`_ctx` 声明处） | 新增 `var use_3d := false`（实例变量，测试可 set） | 总开关；默认撤 3D |
| 2 | `_build_ui`:1673 | `_build_3d_world()` → `if use_3d: _build_3d_world() else: _build_2d_world()` | 停用 3D 场景实例化 |
| 3 | `_build_ui`:1676-1681 | `bg` ColorRect 的 modulate alpha 0 → `_build_2d_world` 设 alpha 1（兜底暗底） | 3D 没了要 2D 兜底色 |
| 4 | 新增 `_build_2d_world()`（~40 行） | 全屏 TextureRect 背景布（`res://assets/ui/battle/backdrops/arena_battlearena1.png`，STRETCH_SCALE + EXPAND_IGNORE_SIZE + MOUSE_FILTER_IGNORE；在 `_build_ui` 中**最先** add → 永远在 HUD/棋盘之下）+ 可选 `_set_2d_backdrop(faction)` 换阵营背景 | 见 §3 方案 A |
| 5 | 新增 `SLOT_RECT_2D` 常量表（36 个 Rect2） | 18 槽固定屏幕矩形（步骤 A 采集硬编码） | 替代 `_cam3d.unproject_position` |
| 6 | `_slot_screen_rect`:862-873 | 开头 `if not use_3d: return SLOT_RECT_2D[p][slot]` | 热区/粒子定位 2D 化 |
| 7 | `_build_board_row`:2083-2159 | 每按钮预建 `CardUI2D` 容器（Art2D/Frame2D/Gem2D/Name2D/Desc2D/Cost/Hp/Melee/Arm 9 个子节点，visible=false） | 2D 场上卡渲染载体 |
| 8 | `_refresh_board`:2647-2725 | 2D 分支：①`u==null`：隐藏 CardUI2D、保留高亮；②有单位：填 CardUI2D（`_art_window_tex`:901 立绘+crop、`_card_frame_path`:942 卡框、GEMS_DIR 宝石、名字/描述、4 数值 Label；位置按矩形相对比例=CARD2D 常量归一化）；face_down→卡背纹理；已有 `art/st/dim/badges` 隐藏如现状 | 单位显示从 3D 卡→2D 卡 |
| 9 | `_update_3d_cards`:991-992 | 开头 `if not use_3d: return` | 3D 刷新停用 |
| 10 | `_set_mode`:4103 | 同 9（或 2D 下改为 `_refresh_boards()`） | 高亮刷新走 2D 路径 |
| 11 | `_refresh_all`:2333 | 同 9 | — |
| 12 | `_center_camera`:2342-2346 | 2D 下 no-op（hint "Camera fixed"） | 无相机可复位 |
| 13 | `_build_ui`:2037-2040 | CenterCamera 按钮 `visible = use_3d` | 2D 无相机 |
| 14 | 设置面板 `_open_battle_settings`:2417-2425 | Auto Zoom 开关 `disabled = not use_3d`（或隐藏） | 原版 Auto Zoom=3D 相机功能 |
| 15 | `_fx3d`:4618-4621 | 2D 分支：`_fx2d(name, _wpos_to_screen(wpos))` | 特效从 GPUParticles3D→GPUParticles2D（UnityParticles.spawn; z_index=30 已有） |
| 16 | 新增 `_wpos_to_screen(wpos) -> Vector2`（~15 行） | 遍历 `_slot_world_pos(p,s)` 匹配 → 返回 `_slot_center(p,s)`；不匹配→全局兜底（`wpos` 出屏滤掉） | **让 15 处 `_fx3d` 调用点（3629/3632/3966/3968/4368-4370/4395/4403/4422-4433/4454/4483-4492/4523）零改动** |
| 17 | 新增 `_fx2d(name: String, pos: Vector2)`（~15 行） | UnityParticles.spawn；无 2D 数据→null 静默（现 spawn 已安全） | 2D 特效挂接 |
| 18 | `_show_damage_number`:4639-4659 | 2D 分支：`screen = _wpos_to_screen(wpos)` + 头顶偏移（-30px 级）；屏外判定同现状 | 伤害数字 2D |
| 19 | `_start_battle`:290-296（注释区） | 2D 模式启用 `_set_2d_backdrop(f_self)`（可选按阵营换背景） | 阵营画面（可选步骤） |
| 20 | L21-54 注释/常量注释 | 更新为 2D 模式说明（保留原 3D 注释） | 文档一致性 |
| 21 | `tools_dev/test_arena_props.gd`:30-32 | `_start_battle` 前加 `_inst.set("use_3d", true)` | 该测试依赖 `_arena_props_root`/`_set_arena_props` |
| 22 | `tools_dev/test_card_vfx.gd`:29-30 | 同上 set use_3d=true | 该测试依赖 `_world3d`+spawn_fx；另可加"2D 模式下 _fx_from_event 无报错"断言 |
| 23 | 新增 `tools_dev/capture_slot_rects.gd`（~30 行，一次性） | 窗口模式跑 battle → 打印 18 槽 `_slot_screen_rect`+`_slot_center` → 生成常量表 | 步骤 A |
| 24 | 新资源 `assets/ui/battle/backdrops/arena_battlearena1.png`（1920×1080） | `shot_arena_alone.gd` 输出 | 方案 A |
| 25 | （可选）~30 个 2D 粒子 JSON 缺失项 | `convert_unity_particles.py` 2D 模式补转：`Healing_Projectile`、`StunEffect_proc`、`Whip Attack EC Maulerfiend`、`BulletImpact_*`(13 阵营)、`Buff_Drachnyen`、`Forewarned Board`、`Godspear Warhead`、`Master Interromancer Hit`、`Rapturous Ruination`、`Cut Wulfen`、`Slash Repeating 2x`、`Rapacious Claw Slash`、`Cut Axe SW`、`Sword Slash` 等 | 2D 特效全量可见（不转则静默跳过） |

### 2.4 需删清单

- **本次零删除**（函数体全保留，`use_3d=false` 即停用）。理由：用户"暂时"放弃 3D，战场任务恢复时从归档取回（CLAUDE.md 约定），保留=零恢复成本。
- 可选后续清理（**不建议本次做**，注释说明即可）：`assets/ui/battle/3d/{background.png, back_background.png, floor.png, floor_ground.png}`（旧手工世界残料；`floor_ground` 仍被 `_arena_floor_tex`:369 引用，而该函数当前无调用链）；`battle.gd.bak_*` 系列备份保留。
- `_build_3d_world` 内 3D 卡层（400-498）与 `_update_3d_cards`（991-1080）在 `use_3d=false` 时**不被执行/被短路**，但代码保留。

---

## 3. 背景方案（关键决策）

### 结论：原版 2D 层**没有背景** → 推荐 **方案 A：一次性烘焙静态背景布**

**依据（§1.1 已核）：** BackCanvas/FrontCanvas 内无任何全屏 Image；`battlearena1_2D层全树.md` 中 Canvas 下第一个元素就是 `Aspect Ratio Filler Top`，随后全是控件。战斗画面的背景 = BoardCamera 视角下的 3D 烘焙场景（`Back background`+`Background`+`Floor plane`+道具+粒子+Sun flare），这是原版唯一的"背景"。

**方案 A（推荐）**：改造前用现有工具 `tools_dev/shot_arena_alone.gd`（SceneTree `_initialize` → instantiate `unity_arena_battlearena1.tscn` → 等 180 帧 → `get_root().get_texture().get_image().save_png()`）在项目内渲染 1 帧 1920×1080 → `assets/ui/battle/backdrops/arena_battlearena1.png` → `_build_2d_world` 全屏 TextureRect 铺布。

- 优点：① 与当前 3D 完全同源——烘焙自动继承 BoardCamera FOV46.397/lensShift(-0.205)/环境/Linear/Glow/LUT+vignette 后处理（CanvasLayer layer=-1 会进截图），视觉=现在画面的冻结帧，即"原版 2D 层画面"；② 一张 PNG=0 运行时 3D 成本；③ **手性/镜像问题退化为 PNG 水平翻转**（视觉验收若与原版相反 → `ImageOps.mirror` 一行解决，use 地图坑 52 就此关账）；④ 工具已存在；⑤ 13 张阵营变体同理可烘（归档 `Warpforge备份/战场演示_暂停归档_0823/渲染截图/out_battlearena*.png` 1920×1080 是旧 demo 工程产物，建议主项目内重烘保证与生成器一致，本次可只烘 battlearena1）。
- 缺点：静态（无相机位移；但原版 BoardCamera 仅在单位行动时轻微推近且 Auto Zoom 已可关——单机复刻下静态可接受，且与用户"换 2D 层"意图一致）；粒子冻结（烟/蒸汽/余烬定格一帧——烘焙时等待 180 帧取"常亮"状态即可，视觉近似原版守静画面）。

**方案 B（备选，仅烘焙不可行时兜底）**：三贴图分层拼布——`Battle Arena 1 Background.png`（中景墙，已存 `assets/textures/`）+ `Battle Arena 1 Back  Background.png`（远景）+ `Battle Arena 1 Floor.png`（地面，底部裁剪）三个 TextureRect 近似摆放。缺点：无道具遮挡/透视关系不对（Barrels/Fences/Vehicle 全丢）→ 不符合复刻一致性，**不推荐**。

**方案 C（否决）**：仅阵营地面贴图 `arena_floors/`（8 阵营）铺底 + 纯色——与原版画面差距最大，仅当 A/B 都不可行时临时。

**阵营差异处理**：当前 battle.gd 的阵营装饰（`_set_arena_props`）已被注释停用（291-295），现游戏所有阵营固定 battlearena1 中立场景 → 2D 版**默认全阵营用一张 battlearena1 背景布**（与现状保持一致）；后续若要做"按阵营换场景"，把 13 张背景布+`SLOT_RECT_2D` 不变（槽位布局是 BattleBoard 层，不随背景变），打开 `_set_2d_backdrop(faction)` 即可——比 3D 版便宜得多。

---

## 4. 逐步改造方案

### 步骤 A（前置采集，只读，预计 30 分钟）
1. 写 `tools_dev/capture_slot_rects.gd`（窗口模式 SceneTree）：instantiate battle.tscn → `_start_battle("Ultramarines","Goff")` → 等 ~30 帧 → 打印 2×9 槽 `_slot_screen_rect()` 与 `_slot_center()` 与单位卡实际屏幕尺寸 → 输出文本常量表。
2. `godot --path D:/warpforge --resolution 1920x1080 --script res://tools_dev/shot_arena_alone.gd`（现有）→ `res://tools_dev/out/port_arena_alone.png` → 拷到 `assets/ui/battle/backdrops/arena_battlearena1.png`。
3. 验证：① PIL 与 `Warpforge备份/战场演示_暂停归档_0823/战场参考图.png`（原版参考）与 `渲染截图/out_battlearena1.png` 逐像素 diff（应低色差——此前 13 场景审计通过）；② vision 整屏对照参考图，确认无镜像/错位；若左右镜像 → PIL `ImageOps.mirror` 后再验。
4. 产出：`SLOT_RECT_2D` 常量 + 背景 PNG（两个新资源）。

### 步骤 B1（开关+背景+槽位热区，不渲卡面，预计半天）
改：表 2.3 #1-6,12-14,19 → 验证：`godot --headless --path D:/warpforge res://tools_dev/test_battle_hud.tscn`、`test_battle_hand.tscn`、`test_battle_defence.tscn`、`test_tutorial.tscn`、`e2e_deck.gd` 全绿；窗口 run → `battle_shot.gd` 整屏截图 → vision 审查：背景完整、槽位热区与原投影吻合（叠加 diff）、部署/攻击/战术高亮、手牌拖拽、END TURN、换牌流程。

### 步骤 B2（2D 场上卡渲染，预计一天）
改：#7,8,9,10,11,20 → 验证：部署后 2D 卡出现（立绘+卡框+宝石+名字+描述+四数值）、受击数值刷新、已行动暗化、敌方卡背、双击详情、卡面高清（PnP face 优先，与手牌一致）、战场局部截图 vision 与"原版 2D 层预期"对照（参照 §1.2 2DCard 模板布局）。

### 步骤 B3（特效+伤害数字 2D，预计半天）
改：#15,16,17,18，可选 #25 → 验证：`test_card_vfx.gd`（先 set use_3d=true 保底原断言）+ 新增 2D 断言（use_3d=false 下 `_fx_from_event` 全事件无报错）；窗口实测：部署召唤圈/攻击命中爆炸/治疗弹/盾甲特效/伤害数字弹跳（Ranged/Melee/Skill 三色拖线已 2D，不动）。**#25 已完成（2026-08-23 战斗线 B3/B4）**：28 名 2D 数据补转（A19/B8/D2，详见使用地图坑 71）+test_card_vfx 2D 段断言（51/51 覆盖+全分支）+shot_fx.gd 32 名截图验收。

### 步骤 B4（收尾，预计半天）
改：#21,22（两个测试 1 行 set 开关）+ 文档注释 + （可选）阵营背景 `_set_2d_backdrop`；回归全清单 §5；整屏 1920×1080 截图 vision 四区审查（顶部栏/侧栏/中央战场/底部手牌），超标项修复复验；更新 `解包资源使用地图.md`（背景布/槽位表/坑 52 关账）+ `项目任务文件.md`。

---

## 5. 风险与回归测试清单

### 5.1 自动化回归
| 测试 | 用法 | 影响 | 处置 |
|---|---|---|---|
| `rule_test` | `godot --headless --path D:/warpforge res://tools_dev/rule_test.tscn` | **零影响**（纯 RuleCore，不跑 battle 场景） | 直接跑，必须绿 |
| `auto_test` | `godot --path D:/warpforge res://tools_dev/auto_test.tscn`（主菜单流程自动化） | 若进入 battle 只查 2D 状态 → 预期不受影响 | 复跑确认 |
| `test_battle_hud` / `test_battle_hand` / `test_battle_defence` / `test_tutorial` | headless 各自 .tscn | 只断言 2D 状态（`_hand_box`/`_hand_drag_data`/`_slot_can_drop`/hint/防御卡）→ 预期不破 | **必须每个绿**；若 B2 改变手牌尺寸断言，同步更新断言 |
| `e2e_deck` | `godot --path D:/warpforge --headless --script res://tools_dev/e2e_deck.gd` | battle 段只查 `_ctx`/`_hand_box` → 预期不破 | 复跑确认 |
| `test_arena_props` | headless .tscn | **依赖 3D**（`_arena_props_root`、`_set_arena_props` 12 阵营节点数、MeshInstance3D 检查）→ 默认 2D 模式下必 FAIL | **必改**：frame 12 前 `_inst.set("use_3d", true)`（1 行）；断言不变 |
| `test_card_vfx` | headless .tscn | **依赖 3D**（`_world3d` + `UnityParticles3D.spawn_fx` 全量加载断言）→ 同上 | **必改**：`_inst.set("use_3d", true)`；另加 2D 分支断言（`_fx_from_event` 在 2D 模式无报错 + 缺数据静默） |

注意：headless 下 `_build_2d_world` 加载 PNG 无 GPU 压力（TextureRect 正常）；`UnityParticles.spawn` 的 headless 兼容与现 `_spawn_scene_particles` 相同（已常态化跑）。

### 5.2 手工回归（每项验收点）
1. **部署**：点击手牌→金框高亮→点格放下（PNG 卡面/立绘+框+宝石+四数值）；拖拽部署（预览 120×160 跟随）；费用不足变暗/拦截。
2. **攻击**：短按己方单位→攻击类型选择器（Melee/Ranged/Skill 单显+Highlight）；拖拽→三钮跟随+准星+三色拖线（红/紫/金）；命中/击杀爆炸/死亡音。
3. **战术**：需目标战术→金框选目标；无需目标→直接结算；Choose one/Energy Activate 弹窗。
4. **回合**：END TURN 倒计时叠钮显示、敌方回合红提示、AI 出牌节奏、己方绿提示。
5. **换牌**：标题 65px/后手副标题/6 张手牌弧形/标记金框/跳过/继续。
6. **HUD**：能量 0-10 弹跳、骷髅计数、牌库计数"Cards left: N/M"、坟场滑入、双击详情、语音按钮（播放/无语音 blocked）、设置（三滑杆持久化、Mute opponent、Resign、Skip Tutorial、Auto Zoom 禁用）。
7. **教程**：6 关提示条逐条（`test_tutorial` + 目视）。
8. **结算**：胜利/失败/平局视频（1920×540 全屏横幅）→ 结果弹窗（骷髅/统计/Play Again/Main Menu）→ Play Again 重开（HUD 恢复）。
9. **杂项**：阵营光标、`_hand_count_label` 超上限才显、中心相机按钮 2D 隐藏、ReplayButtons 仍隐藏。

### 5.3 风险点与缓解
| 风险 | 缓解 |
|---|---|
| SLOT_RECT_2D 采集与真机热区偏移（分辨率/缩放） | 采集脚本同时打印按钮 global rect；改造后 2D 截图与步骤 A 底图叠加 diff（PIL 透明度叠加）；slot 表=固定 1920×1080（项目 viewport 固定，无 Scale 风险） |
| 2D 卡渲染尺寸/字距与 3D 版不一致 | 尺寸=采集矩形（每侧不同）；内部布局=CARD2D 常量归一化（相对矩形比例）；vision 与 §1.2 模板对照；字号：场上卡 ≈30px 值文字/22px 名字 |
| 背景布与 HUD 层级（背景盖住 UI 或反之） | `_build_2d_world` 在 `_build_ui` 最前调用（紧接 bg 之后），后加的一切 2D 在其上；不给背景设 z_index（保持 0）；特效 z=30/手牌 z=10+i/计数 z=20 不受影响 |
| 烘焙帧粒子状态"冻结"尴尬 | 等 180 帧取稳态（爆烟已完成、常亮焰保持）；验收 vision 评分不达标可换帧重烘 |
| 手性（镜像） | 2D 下 = PNG 水平翻转 1 行（PIL），无需 3D 数学；与 `战场参考图.png` 对比后定 |
| 部分 VFX 无 2D 数据 | `UnityParticles.spawn` 无数据返回 null 静默（现实现已安全）；转换脚本补 ~30 个；**禁止为此复活 3D** |
| test_arena_props/test_card_vfx 与 use_3d 默认值耦合 | 两测试显式 set true（#21/22）；若以后彻底删 3D，则这两个测试退役并改 2D 断言 |
| `_fx3d` 分支后 `_slot_world_pos` 仍被诊断工具用 | 函数保留；仅 2D 运行时不再依赖 |

---

## 附：改造后文件清单（一次性产出/修改汇总）

```
新增  d:/warpforge/assets/ui/battle/backdrops/arena_battlearena1.png   (步骤A烘焙, 1920x1080)
新增  d:/warpforge/tools_dev/capture_slot_rects.gd                      (一次性采集; 可随后删除)
修改  d:/warpforge/scripts/battle.gd        (~25处, 净+250~350行; 全部在既有函数内/新增 ≤5 函数)
修改  d:/warpforge/tools_dev/test_arena_props.gd   (1行 set use_3d=true)
修改  d:/warpforge/tools_dev/test_card_vfx.gd      (1行 set use_3d=true + 可选2D断言)
不动  scenes/battle.tscn / scenes/unity_arena_battlearena1.{tscn,gd} / unity_particles.gd /
      unity_particles3d.gd / 全部 assets / 全部其它 scripts
可选  data/particles/*.json 补转 ~30 个 (convert_unity_particles.py --2d)
```
