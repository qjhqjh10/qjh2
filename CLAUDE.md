# Warpforge 解包项目 — 行为准则

## 🔒 安全红线（必须遵守）

1. **`~/.claude.json` 和 `~/.cc-switch/` 绝不同步网盘、不上传、不提交 git**——里面包含 API key 等敏感配置。任何备份/迁移/分享操作都不得包含这两个位置的文件。
2. **API key（MINIMAX_API_KEY 等）不写入任何项目文件、代码、文档、日志**——只从环境变量读取；不打印 key 值；配置里只用 `${MINIMAX_API_KEY}` 占位。
3. 对话中出现的 key 属于敏感信息，不引用、不重复、不写入任何持久化内容。

## 项目结构速览

- `解包整理/` — 解包资源（12 大类，44.9 万文件），入口文档 `解包整理/README.md`
- `Warhammer 40k Warpforge/` — 游戏本体（只读，不修改）
- `Warpforge_code/` — 反编译 C# 代码（查逻辑/枚举/字段用）
- `Warpforge_tools/` — Python 工具集（py312 环境 + 脚本），README 见 `Warpforge_tools/README.md`

## 📖 必读知识文件（新会话先读，避免重复探查）

- **`解包资源使用地图.md`** — 解包资源用途/在用状态/重复损坏/缺失清单/界面元素意义/bundle 清单/工具链（原版游戏怎么组成的知识库）。**做 UI/资源/特效任务必读**；含视频时间线理解（信息已浓缩于此，无需再读视频分析文件）
- `项目任务文件.md` — 活任务清单（完成任务后更新，**每次会话必读**）；末尾"给新会话的情况说明"是交接文档
- `项目资源总览.md` — 顶层目录地图
- 原版游戏视频存档：`原版视频素材/warppforge_orig.mp4`（UI 参照，帧在 hd/ 子目录）——**一次性参考资源**：需要看原版画面时按需抽帧/读 `视频情况解析.txt`，不每次会话重复读

## 📦 资源库规范（D:\2 即资源库）

- **D:\2 是资源库**：解包资源、原版素材、提取产物、备份都放这里，按类型分文件夹（`解包整理/`、`原版视频素材/`、`Warpforge_tools/data/` 等），新资源新建对应类型文件夹，不散落根目录
- 新下载/新生成/新提取的资源：先放资源库对应文件夹，再同步进游戏项目（d:/warpforge/assets/）
- 大文件（视频等）不入 git，路径引用记录在资源使用地图

## 🎨 开发准则（用户指定）

- **优先使用解包资源**：游戏开发（UI/美术/特效/音效）优先从解包资源取用（12 大类 44.9 万文件，大量原版美术资源），只有解包真没有的才用视频帧裁图/生成图替代
- 新发现的资源用途及时写入 `解包资源使用地图.md`

## 常用约定

- 运行脚本用 `d:/2/Warpforge_tools/py312/python.exe`（勿用系统 Python 3.14）
- 卡图/资源路径相对 `d:/2/解包整理/`；卡牌索引 `解包整理/card_index.json`
- 视觉识别：用户要求看图时，走 vision skill（MCP 调用副模型），主模型无法直接看图
- 大文件/资源不动则不动；改动前先备份（用户要求：备份 → 测试 → 通过后删备份）
## Git 仓库

- **唯一上传目标**：`https://github.com/qjhqjh10/qjh2.git`（remote: origin，分支 master）
- 涉及 push/commit 一律推送到 origin/master，不要新建/切换其他远程
- .gitignore 已排除大资源/反编译代码/敏感配置，push 前无需额外处理
